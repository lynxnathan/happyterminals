---
phase: 2.1
slug: obj-mesh-loading
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-15
planned: 2026-04-15
---

# Phase 2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` + `criterion` (benches) + `proptest` (fuzz) + `insta` (snapshots) — all already configured in `crates/happyterminals-renderer/Cargo.toml` |
| **Config file** | `Cargo.toml` per crate; `insta.yaml` at workspace root |
| **Quick run command** | `cargo test -p happyterminals-renderer mesh` |
| **Full suite command** | `cargo test --workspace && cargo bench -p happyterminals-renderer --bench renderer_alloc -- --test` |
| **Estimated runtime** | ~30 seconds quick; ~90 seconds full suite |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p happyterminals-renderer mesh` + `cargo clippy -p happyterminals-renderer -- -D warnings`
- **After every plan wave:** Run `cargo test --workspace && cargo clippy --workspace -- -D warnings`
- **Before `/eclusa:verify-work`:** Full suite must be green including bench-as-test smoke + proptest corpus
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

*Populated during planning. Each must-have gate maps to exactly one owning task.*

| Must-Have Gate | Source | Test Type | Owner Plan → Task |
|----------------|--------|-----------|--------------------|
| 11-file fixture corpus produces `Result<Mesh>` or `Result<MeshError>` — zero panics | RESEARCH §Must-Haves #1 | integration + proptest | 02.1-01 → Task 3 (`corpus_never_panics` in `tests/obj_corpus.rs`) |
| `load_obj("bunny.obj")` returns Mesh with ≥ 4900 triangles, non-empty flat normals | RESEARCH §Must-Haves #2 | unit + integration | 02.1-01 → Task 2 (`load_obj_bunny_has_many_triangles` unit) and Task 3 (`corpus_loads_bunny` integration) |
| `Cube::mesh()` round-trips (12 tris, 8 unique vertices by position, flat normals match face count) | RESEARCH §Must-Haves #3 | unit + integration | 02.1-01 → Task 2 (`cube_mesh_round_trips` unit) and Task 3 (`corpus_cube_mesh_round_trips` integration) |
| Proptest byte-fuzz on synthetic OBJ inputs — zero panics (256 cases CI / 10k opt-in) | RESEARCH §Must-Haves #4 | proptest | 02.1-01 → Task 3 (`load_obj_never_panics_on_arbitrary_bytes` in `tests/obj_corpus.rs`) |
| Rasterizing a loaded bunny Mesh produces ≥ 50 non-space cells on 80×24 grid at default orbit pose | RESEARCH §Must-Haves #5 | unit (renderer) | 02.1-02 → Task 1 (`draw_loaded_bunny_produces_pixels` in `lib.rs` tests) |
| `renderer_alloc` bench extended with bunny case — zero per-frame allocations at 5000-tri scale | RESEARCH §Must-Haves #6 | criterion bench | 02.1-02 → Task 2 (`bench_draw_mesh_bunny` in `benches/renderer_alloc.rs`) plus Task 1's `draw_twice_does_not_grow_z_buffer` capacity-stability proxy |
| `cargo run --example model-viewer` launches, Left/Right arrow keys cycle 3 models, Ctrl-C exits clean | RESEARCH §Must-Haves #7 | compile-only automated + manual UAT | 02.1-03 → Task 2 (`cargo build --example model-viewer` automated; manual verification per `<verification>` block) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/happyterminals-renderer/tests/fixtures/obj/` — directory with 11 OBJ files (3 real + 8 synthetic quirk cases per RESEARCH §Fixture Corpus) — **owner: 02.1-01 Task 1**
- [ ] `crates/happyterminals-renderer/tests/obj_corpus.rs` — integration test harness iterating the corpus + proptest fuzz — **owner: 02.1-01 Task 3**
- [ ] `crates/happyterminals-renderer/benches/renderer_alloc.rs` — extend with bunny-loaded case — **owner: 02.1-02 Task 2**
- [ ] `tobj = { workspace = true }` + `thiserror = { workspace = true }` added to `crates/happyterminals-renderer/Cargo.toml` `[dependencies]`; `tempfile` added to workspace + renderer dev-deps — **owner: 02.1-01 Task 1**
- [ ] `happyterminals::prelude` re-exports for `Mesh`, `load_obj`, `MeshError`, `LoadStats`, `Cube` — **owner: 02.1-03 Task 1**

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Model-viewer visual quality (bunny/cow/teapot render recognizably) | REND-06 | ASCII-output aesthetic judgment — automated snapshots verify shape exists but not "looks like a bunny" | Run `cargo run --example model-viewer -p happyterminals`, press Right arrow twice; confirm three distinct recognizable silhouettes |
| Arrow-key responsiveness feel | REND-06 | Input-latency perception is subjective | Press arrow keys rapidly; no dropped events, no visual stutter |
| Camera auto-fit across differently-sized meshes | RESEARCH §Open Questions #4 | Visual check that no model is too small or clipped | Cycle all three models; each should fill ~60% of visible area |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (fixtures, proptest harness, bench extension)
- [x] No watch-mode flags (`cargo test`, not `cargo watch`)
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter after planner fills Per-Task map

**Approval:** planner-approved (2026-04-15); executor to confirm on first commit.
