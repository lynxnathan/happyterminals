---
phase: 2.3
slug: particles-camera-modes
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-16
updated: 2026-04-16
---

# Phase 2.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` + `proptest` for property tests |
| **Config file** | `Cargo.toml` per crate (standard) |
| **Quick run command** | `cargo test -p happyterminals-input` |
| **Full suite command** | `cargo test --workspace && cargo clippy -p happyterminals-input -- -D warnings` |
| **Estimated runtime** | ~15 seconds quick; ~45 seconds full |

---

## Sampling Rate

- **After every task commit:** `cargo test -p happyterminals-input && cargo test -p happyterminals-renderer -- camera && cargo clippy -p happyterminals-input -- -D warnings`
- **After every plan wave:** `cargo test --workspace`
- **Before `/eclusa:verify-work`:** Full suite green + model-viewer manual smoke test
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

*Each must-have gate is owned by exactly one task. Every gate has an automated verify command that runs in < 20 seconds.*

| Must-Have Gate | Source | Test Type | Owner Plan → Task |
|----------------|--------|-----------|--------------------|
| InputMap dispatch resolves bindings through context stack — top context wins | CONTEXT §InputContext Stack | unit | TBD by planner |
| Drag state machine transitions (Idle→Pressed→Dragging→Released) produce correct Axis2D deltas | CONTEXT §Mouse Drag State Machine | unit | TBD by planner |
| Context push/pop works; default context cannot be popped | CONTEXT §InputContext Stack | unit | TBD by planner |
| Runtime `InputMap::rebind()` changes active bindings immediately | CONTEXT §Runtime Rebinding | unit | TBD by planner |
| Modifier chain (Negate, Scale, Deadzone) applies correctly to action values | CONTEXT §Modifier Types | unit + proptest | TBD by planner |
| Action signals fire on dispatch (reactive graph integration with Signal<T>) | CONTEXT §Reactive Twist | integration | TBD by planner |
| FreeLookCamera + FpsCamera produce correct view matrices via `Camera` trait | CONTEXT §Camera Modes | unit | TBD by planner |
| FpsCamera constrains movement to XZ plane (Y locked to ground_y) | CONTEXT §FPS Camera | unit | TBD by planner |
| TerminalGuard enables `EnableMouseCapture` + `EnableFocusChange` symmetrically | CONTEXT §TerminalGuard | integration | TBD by planner |
| `cargo run --example model-viewer` supports mouse-drag orbit, scroll zoom, WASD pan through InputMap | ROADMAP §Success Criteria #1 | manual UAT | TBD by planner |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/happyterminals-input/Cargo.toml` — new crate setup with workspace deps (crossterm, glam, happyterminals-core)
- [ ] `crates/happyterminals-input/src/lib.rs` — crate root with module declarations
- [ ] `crates/happyterminals-input/tests/` — test files for input_map, drag, context, rebind, modifier, reactive_dispatch
- [ ] `crates/happyterminals-renderer/src/camera.rs` — FreeLookCamera + FpsCamera unit tests added alongside existing OrbitCamera tests

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Mouse drag orbit feels responsive and natural in model-viewer | REND-05 | Perceptual check — responsiveness and feel can't be automated | Run `cargo run --example model-viewer`, left-drag to orbit bunny/cow/teapot, verify smooth rotation with no jitter |
| Camera mode switching produces no visual artifacts | REND-07 | Visual regression check | In model-viewer, press mode-switch key, confirm scene re-renders cleanly without flash or mispositioned geometry |
| Scroll zoom works across terminals (Win Terminal, iTerm2, Kitty) | REND-05 | Cross-terminal behavior differences for scroll events | Run model-viewer in 2+ terminals, scroll to zoom in/out, verify consistent behavior |
| Bunny-orientation fix: bunny fully rotatable via mouse drag | 2.1 UAT finding | User-reported issue — verify the fix | In model-viewer, switch to bunny, mouse-drag orbit to see all angles (top, front, sides) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (new input crate, camera tests)
- [x] No watch-mode flags
- [x] Feedback latency < 20s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ✅ ready for planning (on planner to assign Owner Plan → Task)
