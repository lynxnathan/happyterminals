---
phase: 02.3-particles-camera-modes
plan: 04
type: execute
wave: 3
depends_on: ["02.3-03"]
files_modified: []
autonomous: false
requirements: [REND-05]

must_haves:
  truths:
    - "Mouse left-drag orbits the model smoothly in the model-viewer"
    - "Scroll wheel zooms in/out in the model-viewer"
    - "WASD pans the camera target in the model-viewer"
    - "Left/Right arrows cycle models"
    - "Bunny is fully rotatable via mouse drag (bunny-orientation fix from 2.1 UAT)"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the model-viewer with InputMap-driven controls.

Purpose: Confirm that mouse drag orbit, scroll zoom, WASD pan, and model cycling all work correctly in a real terminal session. This is the phase gate before marking Phase 2.3 complete.

Output: User-confirmed verification of interactive controls.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/phases/02.3-particles-camera-modes/02.3-CONTEXT.md
@.eclusa/phases/02.3-particles-camera-modes/02.3-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify model-viewer interactive controls</name>
  <files></files>
  <action>
    Present the following verification steps to the user. This is a human-verify checkpoint -- no code changes.

    What was built:
    - Complete input action system with InputMap dispatch, context stack, drag state machine, and modifier pipeline.
    - Three camera types (Orbit with elevation clamp, FreeLook, FPS with ground lock).
    - Model-viewer upgraded with mouse-drag orbit, scroll zoom, WASD pan, arrow-key model cycling.
    - All routed through the InputMap action system with reactive signals.

    How to verify:
    1. Run: `cargo run --example model-viewer -p happyterminals`
    2. Mouse drag orbit: Hold left mouse button and drag -- the model should rotate smoothly. Verify azimuth (horizontal drag) and elevation (vertical drag) both work.
    3. Scroll zoom: Scroll wheel up to zoom in (model gets bigger), scroll down to zoom out. Camera distance should change smoothly.
    4. WASD pan: Press W/A/S/D to pan the camera target. Model should shift in the viewport without rotating.
    5. Model cycling: Press Right arrow to cycle to next model (bunny -> cow -> teapot -> bunny). Press Left to go back.
    6. Bunny orientation fix: When viewing the bunny, use mouse drag to orbit it. Verify you can see it from all angles (top, front, sides, back). The bunny should be fully rotatable -- no fixed orientation.
    7. Auto-fit on model switch: When switching models, the camera distance should adjust to fit the new model's size while preserving your azimuth/elevation.
    8. Quit: Press Q or Ctrl-C. Terminal should restore cleanly (no garbage characters, cursor visible, raw mode off).

    Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>cd /home/lynxnathan/code/happyterminals && cargo build --example model-viewer -p happyterminals</automated>
  </verify>
  <done>User confirms: mouse drag orbit is smooth, scroll zoom works, WASD pans, model cycling works, bunny fully rotatable, terminal restores cleanly on exit.</done>
</task>

</tasks>

<verification>
Human-verified: model-viewer interactive controls work as specified.
</verification>

<success_criteria>
User confirms: mouse drag orbit is smooth, scroll zoom works, WASD pans, model cycling works, bunny fully rotatable, terminal restores cleanly on exit.
</success_criteria>

<output>
After completion, create `.eclusa/phases/02.3-particles-camera-modes/02.3-04-SUMMARY.md`
</output>
