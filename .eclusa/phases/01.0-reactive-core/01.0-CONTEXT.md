# Phase 1.0: Reactive Core — Context

**Gathered:** 2026-04-14
**Status:** Ready for planning
**Source:** ROADMAP.md §"Phase 1.0" + STACK.md §1.4 + ARCHITECTURE.md §4 + PITFALLS.md §4, §5, §6, §26

<domain>
## Phase Boundary

**Goal:** Deliver a stable, documented, fine-grained reactive runtime in `happyterminals-core` — the foundation every later phase consumes. Wrap Leptos's `reactive_graph 0.2.13` behind our own `Signal` / `Memo` / `Effect` / `Owner` / `Root` / `batch` / `untracked` / `SignalSetter` types so the public API is ours, not Leptos's, and a migration away from `reactive_graph` later is non-breaking.

**In scope:**
- `happyterminals-core` crate only (other crates stay empty stubs).
- Core reactive types wrapping `reactive_graph 0.2.13`: `Signal<T>`, `Memo<T>`, `Effect`.
- Owner/scope tree: `create_root`, `Owner`, `on_cleanup`.
- `batch(|| { ... })` API.
- Two-phase propagation + runtime cycle detection with clear panic message ("signal cycle A→B→A").
- `Signal::untracked()` / `Memo::untracked()` read APIs.
- Single-threaded runtime + `SignalSetter` channel for cross-thread writes (drained on the render thread).
- `Clock` trait (Production: `SystemClock` — `std::time::Instant`) and `Rng` trait (Production: wrapping `rand::thread_rng`), both injectable for deterministic tests.
- Test suite: diamond-dependency test, cleanup/disposal test, 10k-scene-transition RSS ceiling test, cycle-detection test, cross-thread SignalSetter test, untracked-read test, batch semantics test.
- Public API documentation: every public item has `///` docs; Send/Sync rules stated in module-level doc.
- `REACT-01 … REACT-10` requirements satisfied.

**Out of scope (future phases):**
- Grid / Pipeline / renderer / scene graph — Phases 1.1+
- Actual tachyonfx integration — Phase 1.2
- Any Python bindings — Phase 4.x
- `no_std` support — not planned for v1
- Async runtime selection (tokio vs smol) — deferred to Phase 1.1 (backend perimeter is where it matters)
- Full observer-graph inspector / debugging tool — backlog

</domain>

<decisions>
## Implementation Decisions (locked)

### Crate layout
- All Phase 1.0 code lands in `crates/happyterminals-core/src/`.
- Module structure:
  - `src/lib.rs` — re-exports + module-level docs
  - `src/signal.rs` — `Signal<T>`, `SignalSetter<T>`
  - `src/memo.rs` — `Memo<T>`
  - `src/effect.rs` — `Effect` runner
  - `src/owner.rs` — `create_root`, `Owner`, `on_cleanup`
  - `src/batch.rs` — `batch()`
  - `src/runtime.rs` — thread-local runtime handle + cycle detection
  - `src/clock.rs` — `Clock` trait + `SystemClock`
  - `src/rng.rs` — `Rng` trait + `ThreadRng` (wrapping `rand`)
  - `src/error.rs` — crate error types
- `tests/` — integration tests (diamond, disposal, 10k-transition, cycle, SignalSetter, untracked, batch).

### Dependencies to activate
Add to `crates/happyterminals-core/Cargo.toml` (all inherited from `[workspace.dependencies]`):
- `reactive_graph = { workspace = true }`
- `any_spawner = { workspace = true }` (used in tests only — production API is agnostic)
- `thiserror = { workspace = true }` (for error types)
- `slotmap = "1"` — may be needed if we maintain our own maps of observers; add to `[workspace.dependencies]` if used. Default: start without, add later if needed.
- `rand = "0.9"` — for `ThreadRng` default. Add to `[workspace.dependencies]`.

Dev-dependencies:
- `insta` (snapshot)
- `proptest` (property tests for batch/cycle semantics)
- `criterion` (perf baseline for memo equality-skip)

**No `pyo3`, no `ratatui`, no `tachyonfx` in this crate.** Enforced by doc-lint + workspace structure.

### API surface (user-facing)
Minimal surface — everything else is internal. Keep the public API as small as possible so future migration away from reactive_graph is cheap.

```rust
pub struct Signal<T>;
impl<T: 'static> Signal<T> {
    pub fn new(value: T) -> Self;
    pub fn get(&self) -> T where T: Clone;          // tracks subscription
    pub fn set(&self, value: T);                    // triggers dependents
    pub fn update(&self, f: impl FnOnce(&mut T));   // in-place update
    pub fn untracked(&self) -> T where T: Clone;    // read without subscribing
    pub fn setter(&self) -> SignalSetter<T>;        // Send handle
}

pub struct SignalSetter<T>;    // Send; queues updates
impl<T: 'static + Send> SignalSetter<T> {
    pub fn set(&self, value: T);
    pub fn update(&self, f: impl FnOnce(&mut T) + Send + 'static);
}

pub struct Memo<T>;
impl<T: 'static + Clone + PartialEq> Memo<T> {
    pub fn new(f: impl Fn() -> T + 'static) -> Self;
    pub fn get(&self) -> T;
    pub fn untracked(&self) -> T;
}

pub struct Effect;
impl Effect {
    pub fn new(f: impl FnMut() + 'static) -> Self;  // runs once immediately, then on dep changes
    // Effect is dropped when its Owner is disposed.
}

pub struct Owner;
impl Owner {
    pub fn run_in<R>(&self, f: impl FnOnce() -> R) -> R;
    pub fn dispose(self);
}

pub fn create_root<R>(f: impl FnOnce() -> R) -> (R, Owner);
pub fn on_cleanup(f: impl FnOnce() + 'static);  // must be called inside an Owner scope
pub fn batch<R>(f: impl FnOnce() -> R) -> R;

// Clock + Rng
pub trait Clock: 'static {
    fn now(&self) -> std::time::Instant;
}
pub struct SystemClock;
impl Clock for SystemClock { /* std::time::Instant::now */ }

pub trait Rng: 'static {
    fn gen_u64(&mut self) -> u64;
    fn gen_f32(&mut self) -> f32;
}
pub struct ThreadRng;  // wraps rand::thread_rng
```

### Memo equality-skip (Q1 decision)
- **Decision:** `Memo<T>: PartialEq` bound IS required. Equality-skip behavior is always-on. Rationale: the downstream Effect cost (scene mutation + dirty-flag set) is higher than PartialEq comparison cost for typical scene values (numbers, short strings, small structs); opt-in equality-skip would make the Python bindings surface harder to document.
- Bench harness (criterion) added in this phase to measure the PartialEq overhead on common value types (`f32`, `Vec3`, small struct). Fail CI if PartialEq check takes > 1µs on the benchmark types — unlikely for these, but guards against someone using a big `PartialEq` impl.
- Documented in module-level doc + every Memo docstring.

### Send/Sync model (PITFALLS §6)
- Runtime is **single-threaded**. `Signal<T>`, `Memo<T>`, `Effect`, `Owner` are `!Send` + `!Sync` (phantom data enforces this).
- Cross-thread writes go through `SignalSetter<T>: Send`. Calling `.set(x)` queues the value into an `mpsc` drained on the owning thread at the start of each frame tick (frame scheduling lives in the backend crate — Phase 1.1 — but the drain hook is in core).
- Documented prominently in the crate's module-level doc. Every `Signal` / `Memo` / `Effect` docstring says "Single-threaded. Use `.setter()` for cross-thread writes."
- This is the project's "reactive threading tattoo."

### Cycle detection (PITFALLS §5)
- Runtime tracks the active observer stack during propagation. If an observer's dependency is itself being recomputed higher in the stack, we have a cycle.
- **Behavior:** runtime panics with message `"signal cycle detected: {A} -> {B} -> ... -> {A}"` where labels come from each observer's `debug_label` (optional; defaults to creation source-location via `#[track_caller]` + panic location info).
- Not a `Result` — cycles are programmer errors.
- Test: a signal whose effect writes back to itself must panic with the expected message within the first propagation tick.

### Two-phase propagation (REACT-06)
- Phase 1 (mark dirty): traverse dependency graph, set dirty flags on every dependent Memo/Effect.
- Phase 2 (recompute in topo order): walk dirty nodes in topological order, recomputing Memo values and running Effects.
- `reactive_graph` handles this internally; our wrapper just ensures the semantics are preserved and documented. Our job is the **public surface**, not the internal graph.

### batch() semantics (REACT-05)
- `batch(|| { sig_a.set(1); sig_b.set(2); })` defers propagation until the closure returns. Dependents run once with the combined new state.
- `reactive_graph` provides this; wrap it as `happyterminals_core::batch`.

### Owner tree + on_cleanup (REACT-04)
- `create_root(|| body)` returns `(body_result, Owner)`. Dropping or `.dispose()`-ing the Owner cascades cleanup to every Effect, Memo, and `on_cleanup` registered inside.
- `on_cleanup(f)` registers a fn to run when the current owner is disposed. Panics (in debug) if called outside an owner scope.
- Test: 10k scene transitions (create_root → lots of Memos + Effects → dispose → create_root again) keeps RSS flat within a documented ceiling (e.g., < 10MB growth over 10k cycles). Use `/proc/self/status` or `libc::getrusage` on Linux; skip test on non-Linux with `#[cfg(target_os = "linux")]`.

### Clock + Rng injection (REACT-09)
- Production path uses `SystemClock::default()` / `ThreadRng::default()`.
- Tests construct `ManualClock { now: Cell<Instant> }` and `SeededRng { state: u64 }` to make snapshot tests deterministic.
- Clock/Rng are passed as arguments to functions that need them — NOT global state. This keeps the core crate free of global time/rng.
- Scene code (Phase 1.4+) will accept `&dyn Clock` / `&mut dyn Rng` as params where time/randomness is relevant (e.g., ease functions, particle lifetimes — later phases).

### Error handling
- Public errors via `thiserror`-derived `CoreError { .. }` where applicable.
- `on_cleanup` outside an owner scope: **panic in debug** (`debug_assert!`), **no-op in release** with a `tracing::warn!`. Documented tradeoff.
- Cycle detection: always panic (cycles are bugs).

### Testing strategy (REACT-10)
- Unit tests inline in each module for isolated logic.
- Integration tests in `crates/happyterminals-core/tests/`:
  - `diamond.rs` — A signal feeding two Memos that both feed one Effect runs the Effect exactly once per `A.set()`.
  - `disposal.rs` — dropping an Owner disposes all its Effects (including via `on_cleanup`).
  - `transitions_10k.rs` — 10k create_root→dispose cycles, assert RSS stays under ceiling (Linux only).
  - `cycle.rs` — self-cycling signal panics with the expected message.
  - `signal_setter.rs` — spawn a thread that holds a `SignalSetter`, write to it, observe value updates after `drain()` on the main thread.
  - `untracked.rs` — reading a signal via `.untracked()` inside an Effect does NOT create a subscription.
  - `batch.rs` — `batch` coalesces multiple sets into a single propagation pass.
- Property tests (`proptest`):
  - `set()` then `get()` on a signal always returns the last-set value (trivial but sanity).
  - Any sequence of nested `batch`es is equivalent to one flat batch wrapping all the sets.
- Criterion bench:
  - `Memo<f32>` equality-skip micro-benchmark — establish the < 1µs overhead baseline.
- Doc-tests on every public item where feasible.

### Clippy + unsafe policy
- `#![forbid(unsafe_code)]` at `lib.rs` top.
- `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]` — panics only via the explicit cycle-detection path (`unreachable!` / `std::panic::panic_any`) and `debug_assert!`.
- All `.workspace = true` lints from Phase 0 apply.

### Claude's Discretion
- Exact phantom-data type to enforce `!Send`/`!Sync` (`PhantomData<*const ()>` is canonical; use it).
- Exact label/debug-name plumbing for cycle detection messages — implementer chooses whether to use `#[track_caller]` + `Location::caller()` or explicit `.label("name")` methods (recommend: both, with `.label()` overriding the caller-location default).
- Exact allocation strategy for observer graph — `reactive_graph` handles it internally; we don't re-invent.
- Whether to expose a `create_rw_signal` split (Leptos style) — recommend NOT for now; keep `Signal::new` unified.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Project context
- `.eclusa/PROJECT.md` — identity, core value, key decisions
- `.eclusa/REQUIREMENTS.md` — REACT-01..10 definitions
- `.eclusa/ROADMAP.md` §"Phase 1.0" — authoritative phase spec
- `.eclusa/STATE.md` — current focus + open questions

### Research foundations
- `.eclusa/research/STACK.md` §1.4 — reactive_graph rationale + version pins
- `.eclusa/research/ARCHITECTURE.md` §3 (data flow), §4 (reactive core design), §12 (anti-patterns — especially "renderer must use untracked"), §14 Q1 (Memo PartialEq)
- `.eclusa/research/PITFALLS.md` §4 (owner tree), §5 (over/under-fire + cycle detection), §6 (Send/Sync), §26 (Clock + RNG injection)
- `.eclusa/research/SUMMARY.md` §"Milestone Sketch — Phase M1.0"

### External references
- Leptos `reactive_graph` 0.2.13: https://docs.rs/reactive_graph/0.2.13/
- Leptos reactivity guide (applies conceptually): https://book.leptos.dev/reactivity/
- SolidJS reactivity model (the conceptual source): https://docs.solidjs.com/advanced-concepts/fine-grained-reactivity
- `any_spawner` docs: https://docs.rs/any_spawner/
- `rand 0.9` — for `ThreadRng`: https://docs.rs/rand/0.9/

</canonical_refs>

<specifics>
## Specific Concerns

### Current state
- `crates/happyterminals-core/src/lib.rs` is an empty doc-comment scaffold (Phase 0 cleaned it up).
- `Cargo.toml` has zero `[dependencies]` — Phase 0 stripped them.
- `[workspace.dependencies]` already pins `reactive_graph 0.2.13`, `any_spawner 0.3`, `thiserror 2.0`, `insta 1.47`, `proptest 1.11`, `criterion 0.8`.
- Need to ADD `rand = "0.9"` to `[workspace.dependencies]` during this phase (currently not pinned).
- `slotmap` may or may not be needed — plan starts without, add only if `reactive_graph`'s surface forces us.

### Public API stability commitment
- Everything in this phase's public surface ships under `happyterminals-core 0.0.0` (pre-release). Semver discipline kicks in at `0.1.0` (Phase 3.5 publish). Until then, breaking changes are allowed — but we aim to avoid them to keep Phase 1.1+ stable.

### Reactive API naming departures from Leptos
- We use `Signal::new()` / `.get()` / `.set()` / `.update()` / `.untracked()` / `.setter()` — NOT Leptos's `create_signal` / `ReadSignal` + `WriteSignal` split. This is the main intentional departure.
- `Memo::new()` — NOT `create_memo`.
- `Effect::new()` — NOT `create_effect`.
- `create_root` / `Owner` — we keep Leptos's names because `reactive_graph` exposes them directly.
- `batch` / `on_cleanup` — same.

### Explicitly declined alternatives
- **Not building our own reactive graph from scratch.** Wrapping `reactive_graph` is the explicit STACK §1.4 recommendation. Rationale: correctness of two-phase propagation + cycle detection is tricky; re-proving it is a 3-month tangent.
- **Not exposing `reactive_graph` types in our public API.** Future migration (or replacement with our own impl) must be non-breaking for downstream crates. Wrapper types MUST hide the underlying graph.

</specifics>

<deferred>
## Deferred to Later Phases

- Async-ness of the frame drain loop — Phase 1.1 (backend) owns the tokio::select integration. Core only provides the `drain_setter_queue()` hook.
- Particle-system-specific signal optimizations (e.g., many-many tiny signals) — Phase 2.3 (particles).
- Python-side Send/Sync implications — Phase 4.2.
- Full `tracing` integration in reactive propagation — out of scope here; add targeted `trace!` spans only at the setter-drain boundary.
- Custom observer-graph debug visualizer — backlog.

</deferred>

---

*Phase: 01.0-reactive-core*
*Context gathered: 2026-04-14 — extracted from ROADMAP/SUMMARY/ARCHITECTURE/PITFALLS; no discuss-phase needed (scope fully pre-specified).*
