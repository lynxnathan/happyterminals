---
phase: 03.4
slug: examples-library
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-17
---

# Phase 03.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo (Rust) + custom shell smoke script |
| **Config file** | Cargo.toml (workspace) + scripts/smoke-examples.sh (new, Wave 0) |
| **Quick run command** | `cargo check -p happyterminals --example <name>` |
| **Full suite command** | `cargo test --workspace && bash scripts/smoke-examples.sh` |
| **Estimated runtime** | ~60 seconds (tests) + ~30 seconds (smoke compile) |

---

## Sampling Rate

- **After every task commit:** Run `cargo check -p happyterminals --example <name>` for the touched example
- **After every plan wave:** Run `cargo test --workspace` + `bash scripts/smoke-examples.sh`
- **Before `/eclusa:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.4-01-01 | 01 | 1 | REL-04 | build | `cargo check -p happyterminals` | ✅ | ⬜ pending |
| 03.4-02-01 | 02 | 1 | REL-04 | build+smoke | `cargo check -p happyterminals --example text-reveal` | ❌ W0 | ⬜ pending |
| 03.4-03-01 | 03 | 1 | REL-04 | build+smoke | `cargo check -p happyterminals --example json-loader` | ❌ W0 | ⬜ pending |
| 03.4-04-01 | 04 | 2 | DEMO-05 | doc-lint | `bash scripts/doc-lint-examples.sh` | ❌ W0 | ⬜ pending |
| 03.4-05-01 | 05 | 2 | REL-04 | smoke-all | `bash scripts/smoke-examples.sh` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/smoke-examples.sh` — `cargo check --example X -p happyterminals` for each of the 5 headline examples (fail-fast with `set -euo pipefail`)
- [ ] `scripts/doc-lint-examples.sh` — verify every example's `main.rs` contains a `//!` header block with required sections (purpose / features exercised / controls / run command)
- [ ] `.github/workflows/ci.yml` (or equivalent) addition — run `scripts/smoke-examples.sh` and `scripts/doc-lint-examples.sh` on PR

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| text-reveal lands the "holy shit" visual moment | REL-04 / hero criterion | Visual quality and timing can only be judged by human eye (existing `feedback_visual_iteration.md` pattern) | `cargo run -p happyterminals --example text-reveal` — confirm bunny visible + text reveal animates smoothly + spacebar replays + Tab swaps effect |
| json-loader sandbox story reads clearly | REL-04 / DSL-08 narrative | The commented-out bad path needs to be discoverable and the error (when un-commented) readable by a human | Read example source; uncomment the bad-path line; `cargo run -p happyterminals --example json-loader` — confirm stderr prints a clear rejection message |
| All example header blocks are consistent & useful | DEMO-05 | Style consistency and reader clarity is subjective | Skim all 7 example headers side-by-side; confirm they follow the model-viewer pattern |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (smoke-examples.sh, doc-lint-examples.sh, CI hookup)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter after planner sign-off

**Approval:** pending
