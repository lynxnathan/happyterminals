# Phase 3.3: JSON Sandbox - Context

**Gathered:** 2026-04-17
**Status:** Ready for planning
**Mode:** Infrastructure phase — discuss skipped

<domain>
## Phase Boundary

Add security sandboxing to the JSON recipe loader: static effect registry (no eval/shell-out), path sandboxing (configurable asset root), and ANSI-injection stripping on user-provided strings. Covers DSL-05, DSL-08.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices are at Claude's discretion — pure security infrastructure phase. Key constraints from ROADMAP success criteria:
- Effect names resolve through a static registry — unregistered names return error
- Mesh paths sandboxed to a configurable root — path traversal rejected before I/O
- ANSI escapes in user strings stripped — literal characters rendered, not escape sequences
- Implementation in `happyterminals-dsl` alongside the JSON loader from Phase 3.2

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `crates/happyterminals-dsl/src/json.rs` — `load_recipe()` is the integration point. Sandboxing wraps or extends this.
- `crates/happyterminals-scene/src/transition_effect.rs` — built-in effects (Dissolve, SlideLeft, FadeToBlack) are the static registry candidates.

### Integration Points
- **Modified:** `crates/happyterminals-dsl/src/json.rs` — add sandbox checks to load_recipe path resolution and effect name resolution.
- **New:** sandbox utility functions (path canonicalization + ANSI stripping).

</code_context>

<specifics>
## Specific Ideas

No specific requirements — infrastructure phase. Refer to ROADMAP success criteria.

</specifics>

<deferred>
## Deferred Ideas

None.

</deferred>
