---
phase: 2.2
slug: color-mode-pipeline
status: passed
verified: 2026-04-16
uat_confirmed: 2026-04-16
score: 8/8 must-haves verified + human UAT confirmed
re_verification:
  is_re_verification: false
human_verification:
  - test: "Windows Terminal 16-color legibility (ROADMAP success criterion #2)"
    expected: "`cargo run --example color-test -p happyterminals -- --force-color=16` on Windows Terminal shows 8+ legible system colors (R/G/B/Y/M/C/W) on the named-16 row + bold modifiers preserved on row 6. Cube/bunny snapshots already prove ANSI-16 emits only named variants — this is the pixel-eyeball confirmation."
    why_human: "Cross-platform visual rendering quality (font, theme, contrast) cannot be asserted programmatically; the codebase already proves the byte-level contract."
  - test: "Visual color degradation perception across modes"
    expected: "`cargo run --example color-test -p happyterminals -- --force-color={truecolor,256,16,none}` produces progressively-degraded but recognizable output. Each mode looks correct (no wrong-hue artifacts)."
    why_human: "Perceptual quality / wrong-hue detection requires human eyeballs."
  - test: "tmux Tc passthrough on a live tmux session"
    expected: "Inside tmux WITH `set -ga terminal-overrides ',*256col*:Tc'`, `cargo run --example color-test -p happyterminals` auto-detects TrueColor (header shows `auto-detect`, RGB gradient row 2 displays smoothly). Without `Tc`, falls back to Palette256 banding."
    why_human: "Multi-process env propagation through tmux cannot be CI-mocked."
  - test: "`NO_COLOR=1` + `--force-color=truecolor` → mono at runtime (belt-and-suspenders)"
    expected: "`NO_COLOR=1 cargo run --example color-test -p happyterminals -- --force-color=truecolor` produces monochrome output (all RGB gradient cells default-fg, modifier row preserved). NO_COLOR wins over the programmatic override at the real-binary level."
    why_human: "Unit + integration tests already cover this priority (`no_color_beats_override` in both `tests/color_detect.rs` and inline `color.rs`); manual run is the regression gate against the live `run()` entry path."
---

# Phase 2.2 — Color-Mode Pipeline — Verification Report

**Phase Goal:** Make color output safely degrade across the full terminal capability spectrum (truecolor → 256 → 16 → monochrome) with explicit user control via `NO_COLOR` and `--force-color`, so the library never outputs a garbled color sequence on a terminal that can't parse it.

**Verified:** 2026-04-16
**Status:** **human_needed** — every automated gate is green; only Windows-Terminal visual confirmation and tmux/multi-process UAT remain. The byte-level contract is proven by snapshots; only human-eye verification of the rendered pixels is left.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `NO_COLOR=1` produces zero SGR color escapes on flush | ✓ VERIFIED | `tests/color_detect.rs::no_color_strips_all_chromatic` green; both mono `.snap` fixtures show all `fg: Reset, bg: Reset`; `grep "fg: (Rgb|Indexed|<any-named>)" cube_mono.snap` → 0; `grep` on `bunny_mono.snap` → 0 |
| 2 | Forcing Ansi16 on truecolor terminal emits only 4-bit color codes | ✓ VERIFIED (byte-level) / ⚠ HUMAN (Windows-Terminal pixels) | `cube_ansi16.snap` + `bunny_ansi16.snap` contain only named-16 variants (27 hits in cube, all in {Yellow, DarkGray, Gray, Reset}); `grep "Rgb\|Indexed" cube_ansi16.snap` → 0; `grep "Rgb\|Indexed" bunny_ansi16.snap` → 0. Cross-platform pixel-legibility check is human UAT. |
| 3 | Docs explain the tmux `tmux-256color` + `Tc` flag combo | ✓ VERIFIED | README.md lines 77-98 contain verbatim `set -g default-terminal "tmux-256color"` + `set -ga terminal-overrides ",*256col*:Tc"` config, rationale for why it's needed, and the failure-mode description. |
| 4 | Snapshot tests for the spinning cube exist at every color mode | ✓ VERIFIED | 4 cube snapshots (`cube_truecolor`, `cube_palette256`, `cube_ansi16`, `cube_mono`) + 4 bunny snapshots = 8 fixtures in `tests/snapshots/`. `cargo test -p happyterminals-backend-ratatui --test color_snapshots` → **8 passed, 0 failed**. Determinism: second run produces 0 `.snap.new` files (Pitfall 5 closed). |

**Score:** 4/4 ROADMAP success criteria byte-verified; 1 (criterion #2) needs additional human visual confirmation on Windows Terminal.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/happyterminals-backend-ratatui/src/color.rs` | `ColorMode` enum + `EnvProvider` trait + `detect_color_mode` cascade + `downsample(&mut Buffer, ColorMode)` | ✓ VERIFIED | 573 LOC; all 4 ColorMode variants present; cascade order matches spec; downsample iterates via `cell_mut(Position)` preserving Modifier bits |
| `crates/happyterminals-backend-ratatui/src/color/palette.rs` | `PALETTE_256` + `PALETTE_16_LUT` as `const fn` arrays | ✓ VERIFIED | 232 LOC; `pub const PALETTE_256: [(u8, u8, u8); 256] = build_palette()`; `pub const PALETTE_16_LUT: [u8; 256] = build_16_lut()`; xterm anchors asserted (idx 0/15/16/231/232/255) |
| `crates/happyterminals-backend-ratatui/src/frame_spec.rs` | `color_mode: Option<ColorMode>` field; default `None` | ✓ VERIFIED | Field at line 22; `Default` impl returns `color_mode: None`; tests `test_default_color_mode` + `test_explicit_color_mode` cover both paths |
| `crates/happyterminals-backend-ratatui/src/guard.rs` | `TerminalGuard` caches `ColorMode`; `acquire_with_color_mode(mode)` constructor | ✓ VERIFIED | `pub color_mode: ColorMode` field; `acquire_with_color_mode` constructor + back-compat `acquire()` shim; `test_guard_caches_color_mode` exercises 4 modes |
| `crates/happyterminals-backend-ratatui/src/run.rs` | Flush hook in `run()` AND `run_scene()` | ✓ VERIFIED + WIRED | Both functions detect once at entry (lines 54, 151), pass mode to `TerminalGuard::acquire_with_color_mode` (lines 57, 154), and call `downsample(&mut out, color_mode)` inside the `terminal.draw(\|frame\| {...})` closure (lines 84, 205). Detection is OUTSIDE the per-frame loop — no per-frame env reads. |
| `crates/happyterminals-backend-ratatui/tests/color_snapshots.rs` | 8 `insta` snapshots: 2 scenes × 4 modes | ✓ VERIFIED | 178 LOC; 8 `#[test]` functions, all calling `insta::assert_debug_snapshot!(buf)` (per Pitfall 5 — Buffer Debug, not raw ANSI bytes); 8 `.snap` baselines on disk (922B → 15.2K range matching mode color density) |
| `crates/happyterminals-backend-ratatui/tests/color_detect.rs` | Cascade integration tests via `EnvProvider` injection | ✓ VERIFIED | 119 LOC; `FakeEnv` test double (no `std::env::set_var`); 11 integration tests covering NO_COLOR priority, override priority, COLORTERM truecolor/24bit/legacy, TERM=256/dumb/empty |
| `crates/happyterminals/examples/color-test/main.rs` | Demo with `--force-color={truecolor,256,16,none}` CLI arg | ✓ VERIFIED | 121 LOC; `parse_force_color()` handles the 4 documented values + clean exit-2 on invalid; uses ONLY `happyterminals::prelude::*` (validates Plan 02 prelude completeness end-to-end) |
| `crates/happyterminals/README.md` §Terminal Color Support | Cascade docs + tmux `Tc` config + cross-link to color-test | ✓ VERIFIED | Lines 8-110: cascade priority enumerated, FrameSpec usage example, verbatim tmux config block, cross-link to `examples/color-test/main.rs`, external refs (no-color.org, termstandard/colors, sunaku) |
| `crates/happyterminals/src/lib.rs` prelude re-export | `ColorMode` accessible at `happyterminals::prelude::ColorMode` | ✓ VERIFIED | Line 33: `pub use happyterminals_backend_ratatui::ColorMode;`; `prelude_reexports_compile` test references `ColorMode` via `type_name` (line 85) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `run()` entry | `detect_color_mode_from_real_env` | direct call (line 54) | ✓ WIRED | Result bound to `color_mode`, threaded through `acquire_with_color_mode` and per-frame `downsample` |
| `run_scene()` entry | `detect_color_mode_from_real_env` | direct call (line 151) | ✓ WIRED | Same pattern as `run()` |
| `terminal.draw` closure (in `run`) | `downsample(&mut out, color_mode)` | line 84 | ✓ WIRED | After `grid.deref().clone()`, before `*frame.buffer_mut() = out` — single locus of mode enforcement |
| `terminal.draw` closure (in `run_scene`) | `downsample(&mut out, color_mode)` | line 205 | ✓ WIRED | Fires AFTER pipeline so effect-emitted truecolor is downsampled too (key correctness property) |
| `FrameSpec.color_mode` | `detect_color_mode_from_real_env(spec.color_mode)` | line 54/151 — `Option<ColorMode>` flows in as the override | ✓ WIRED | `None` → autodetect; `Some(x)` → forced (modulo NO_COLOR override) |
| `happyterminals::prelude` | `ColorMode` | re-export chain `backend_ratatui::ColorMode` ← `pub use happyterminals_backend_ratatui::ColorMode` | ✓ WIRED | color-test example proves end-to-end import via `use happyterminals::prelude::*` only |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `downsample` (in flush path) | `cell.fg` / `cell.bg` mutated in place | input `Buffer` from `grid.deref().clone()` | YES — real Grid → real Buffer; render pipeline writes Color values that downsample then transforms | ✓ FLOWING |
| `cube_ansi16.snap` | post-downsample Buffer state | `build_cube_scene()` → renderer.draw + RGB injection → downsample(Ansi16) | YES — 27 styled cells emitted, all in named-16 set; mode is mode-discriminating not stub | ✓ FLOWING |
| `cube_mono.snap` | post-downsample Buffer state | same → downsample(Mono) | YES — 1 styles entry (the all-Reset default); proves chromatic strip is total | ✓ FLOWING |
| `color-test` example header row | `header_mode_str` + `provenance` | `parse_force_color()` returns `Option<ColorMode>` from real `std::env::args()` | YES — string flows into `grid.put_str` at row 0 | ✓ FLOWING |
| `color-test` RGB gradient row | per-column RGB style | computed inline from `col / width` (deterministic visual gradient) | YES — Color::Rgb varies across columns, then run() downsamples per active mode | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Color snapshot suite passes deterministically | `cargo test -p happyterminals-backend-ratatui --test color_snapshots` | 8 passed, 0 failed (0.03s) | ✓ PASS |
| Determinism — re-run produces zero `.snap.new` | second invocation of above | 0 `.snap.new` on disk | ✓ PASS (Pitfall 5 closed) |
| Workspace test suite green | `cargo test --workspace` | 257 passed, 6 ignored (38 suites, 1.04s) | ✓ PASS |
| Phase-touched-crate clippy clean | `cargo clippy -p happyterminals-backend-ratatui -p happyterminals --lib --bins --examples -- -D warnings` | No issues found | ✓ PASS |
| Existing examples still build | `cargo build --example spinning-cube -p happyterminals` and `--example model-viewer -p happyterminals` | both clean (0 crates compiled — already built; no regression) | ✓ PASS |
| color-test example builds | `cargo build --example color-test -p happyterminals` | clean | ✓ PASS |
| REND-09 alloc bench preserved | `cargo bench -p happyterminals-renderer --bench renderer_alloc -- --test` | `renderer_draw_80x24 Success`, `renderer_draw_bunny_80x24 Success` | ✓ PASS |
| color-test runs end-to-end on a TTY | `cargo run --example color-test -p happyterminals -- --force-color=…` | requires real TTY | ? SKIP — covered by manual UAT row 1-4 |

---

## Must-Have Gate Verification (from VALIDATION.md)

| # | Must-Have Gate | Owner | Status | Evidence |
|---|----------------|-------|--------|----------|
| 1 | `NO_COLOR=1` produces zero SGR color escape sequences on flush | 02.2-01 T2 + T3 | ✓ GREEN | `downsample_mono_strips_*` (4 inline tests in color.rs) + `no_color_strips_all_chromatic` (color_detect.rs) all green |
| 2 | `$COLORTERM=truecolor` → `ColorMode::TrueColor` (after NO_COLOR) | 02.2-01 T2 + T3 | ✓ GREEN | `colorterm_truecolor` + `colorterm_24bit` + `colorterm_truecolor_uppercase` (inline) + `colorterm_truecolor`/`colorterm_24bit` (integration) |
| 3 | `FrameSpec.color_mode = Some(Ansi16)` on truecolor terminal emits only 4-bit codes | 02.2-03 T1 | ✓ GREEN | `cube_ansi16.snap` + `bunny_ansi16.snap` contain only named-16 variants (greppable: 0 `Rgb`, 0 `Indexed`, 27 named) |
| 4 | RGB→256 Euclidean lookup matches xterm canonical for 6×6×6 cube + 24 greys | 02.2-01 T1 | ✓ GREEN | 9 inline palette tests (`palette_cube_origin`, `_corner_white`, `_mid_sample`, `palette_grey_first`, `palette_grey_last`, `nearest_256_*`) + xterm anchors verified |
| 5 | 256→16 LUT builds at compile time + round-trip stable | 02.2-01 T1 | ✓ GREEN | `pub const PALETTE_16_LUT: [u8; 256] = build_16_lut()` (compile-time proof); `palette_16_lut_range` + `palette_16_lut_white_maps_to_white_or_light_grey` tests green |
| 6 | 8 `insta` snapshots (4 modes × 2 scenes) pass deterministically on CI | 02.2-03 T1 | ✓ GREEN | All 8 `.snap` files present; suite passes; second-run determinism confirmed (0 `.snap.new`) |
| 7 | `cargo run --example color-test -- --force-color=none` produces monochrome output with modifiers preserved | 02.2-03 T2 + manual UAT | ✓ COMPILE-GREEN / ⚠ HUMAN-RUN | Compile gate green (`cargo build --example color-test -p happyterminals` clean); behavior implied by `downsample_mono_preserves_modifier_*` unit tests; manual run is human UAT (row 4 in human_verification block) |
| 8 | Mono mode drops fg/bg but preserves `Modifier::{BOLD, ITALIC, UNDERLINED, REVERSED}` | 02.2-01 T2 | ✓ GREEN | `downsample_mono_preserves_modifier_bold` + `downsample_mono_preserves_modifier_underline_reverse` inline tests; `downsample_mono_strips_named_to_reset` confirms fg/bg strip |

**Score:** 8/8 must-haves automated-verified (gate 7's compile gate is automated; the human-run row is acknowledged in `human_verification`).

---

## Deferral Discipline (CONTEXT §Deferred Ideas)

| Deferred Item | Still Deferred? | Evidence |
|---------------|-----------------|----------|
| Per-cell color mode overrides | ✓ YES | No per-cell API surfaced; downsample is uniform across the buffer |
| Oklab / perceptually-uniform color distance | ✓ YES | `grep -r oklab\|palette::Lab` in backend-ratatui → 0 matches; `sq_dist` is sRGB Euclidean |
| Automatic tmux probe / `$TMUX` handling | ✓ YES | Cascade reads only `NO_COLOR`/`COLORTERM`/`TERM`; no `$TMUX` reference. README documents user-config Tc fix instead |
| Theme / palette customization | ✓ YES | `PALETTE_256` is a `pub const`; no setter API |
| Auto-fallback chain (silent downgrade) | ✓ YES | Cascade is literal — Some(mode) is respected (modulo NO_COLOR per spec) |
| Background color strategies | ✓ YES | bg handled identically to fg in `map_color`; no transparency code |
| Zero new external deps | ✓ YES | `grep supports-color\|oklab\|palette::\|^clap \|^argh \|^structopt ` across all Cargo.toml → 0 matches; only the existing workspace `insta` (Plan 01 added it to backend-ratatui dev-deps) |

---

## Pitfall Closure (RESEARCH §Pitfalls)

| Pitfall | Description | Status | Evidence |
|---------|-------------|--------|----------|
| 4 | No `std::env::set_var` in tests (parallel env race) | ✓ CLOSED | `grep std::env::set_var crates/happyterminals-backend-ratatui/tests/` → 1 doc-comment match (`"//! \`std::env::set_var\` (RESEARCH §Pitfall 4...)"`); 0 actual call sites. `EnvProvider` + `FakeEnv` injection used instead. |
| 5 | Snapshot must use Buffer Debug, not raw ANSI bytes (diff-order non-determinism) | ✓ CLOSED | `tests/color_snapshots.rs` uses `insta::assert_debug_snapshot!(buf)` exclusively (8 occurrences); 0 `assert_snapshot!(...bytes...)`. Determinism proven by second-run (0 `.snap.new`). |
| 6 | No per-frame logging in render loop | ✓ CLOSED | `grep tracing::debug!\|tracing::info!\|println!` in `run.rs` → 2 matches, both `tracing::debug!(?color_mode, "color-mode detected")` at run() entry (line 55, 152) — ABOVE the `loop {}` body. Per-frame `_span = tracing::trace_span!("frame").entered()` is span enter only (no log emit). |
| 7 | Mono uses `Color::Reset`, not `Color::Black` | ✓ CLOSED | `Color::Black` appears in `color.rs` only at: (a) doc comment line 118 (warning against the bad pattern), (b) doc comment line 147 (same), (c) `named_from_u8` line 169 (canonical 16-color mapping for index 0 — different concern), (d-e) test fixtures. The `Mono` arm of `map_color` is the explicit single line `ColorMode::Mono => Color::Reset,` (line 149). Inline `downsample_mono_strips_named_to_reset` test passes a `Color::Red` cell and asserts `cell.fg == Color::Reset` post-downsample. |

---

## Lint + Test State

| Command | Result |
|---------|--------|
| `cargo test --workspace` | **257 passed, 6 ignored** (38 suites, 1.04s) |
| `cargo test -p happyterminals-backend-ratatui --test color_snapshots` | **8 passed, 0 failed** (0.03s) — determinism: 0 `.snap.new` on second run |
| `cargo clippy -p happyterminals-backend-ratatui -p happyterminals --lib --bins --examples -- -D warnings` | **No issues found** (clean on phase-touched crates) |
| `cargo build --example spinning-cube -p happyterminals` | ✓ clean |
| `cargo build --example model-viewer -p happyterminals` | ✓ clean |
| `cargo build --example color-test -p happyterminals` | ✓ clean |
| `cargo bench -p happyterminals-renderer --bench renderer_alloc -- --test` | ✓ `renderer_draw_80x24 Success` + `renderer_draw_bunny_80x24 Success` (REND-09 preserved) |

**Pre-existing workspace `--all-targets` clippy debt** (in unrelated test files) is documented in `deferred-items.md` and is OUT OF SCOPE for Phase 2.2 — confirmed by lib-only / phase-touched-crate scoped clippy being clean.

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REND-08 | 02.2-01 + 02.2-02 + 02.2-03 (all three claim it; this is intended — they decompose the same requirement across foundation/wiring/exit-artifacts) | Color-mode pipeline: RGB → 256 → 16 → mono fallback; `NO_COLOR` env honored; `--force-color` override; tmux `Tc` truecolor guidance in docs | ✓ SATISFIED | All 4 ROADMAP success criteria byte-verified; cascade + downsample shipped; `--force-color` CLI in color-test; tmux config in README |

No orphaned requirements: REQUIREMENTS.md maps Phase 2.2 → REND-08 only; all three plans claim REND-08 (foundation/wiring/exit decomposition).

---

## Anti-Patterns Found

None blocking.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tests/snapshots/*` | n/a | (n/a) | ℹ INFO | Snapshot fixtures intentionally contain `Color::Reset` and `Color::Rgb(...)` — these are test data, not stub indicators |
| `src/color.rs` | 169 | `0 => Color::Black` in `named_from_u8` | ℹ INFO | Canonical ANSI-16 index-0 mapping; NOT used in Mono mode (verified by inline test). Pitfall 7 closure intact. |

No `TODO`/`FIXME`/`PLACEHOLDER` in Phase 2.2 source files (`grep -rE "TODO\|FIXME\|XXX\|HACK\|PLACEHOLDER" src/color.rs src/color/palette.rs src/frame_spec.rs src/guard.rs src/run.rs tests/color_*.rs examples/color-test/main.rs` → 0 matches).

---

## Backward Compatibility

- `spinning-cube` example: builds clean without code changes (uses `..FrameSpec::default()` so `color_mode: None` is the default).
- `model-viewer` example: builds clean without code changes.
- `TerminalGuard::acquire()` retained as a back-compat shim returning `acquire_with_color_mode(ColorMode::TrueColor)` — ~18 existing test call sites unchanged.

---

## Recommendation

**Phase 2.2 ready to close → advance to Phase 2.3 (Particles + Camera Modes).**

Every automated must-have gate is green. Every CONTEXT deferral remains deferred. Every RESEARCH pitfall is closed by code or test. REND-08 is fully satisfied at the byte-contract level; the only outstanding work is human visual UAT (Windows Terminal pixel-legibility, perceptual degradation across modes, live-tmux Tc passthrough, and the NO_COLOR-beats-override real-binary regression check). These four UAT items are explicitly enumerated in the `human_verification:` frontmatter for the user to walk through — the expected outcome of each is a clean visual confirmation, not new code.

If any UAT item fails, gaps would be auto-routable via `/eclusa:plan-phase --gaps`; based on the byte-level evidence (all 8 snapshots pass, modifier-preservation tests green, named-16 closure proven), no failure is anticipated.

**Status downgrade rationale (`human_needed`, not `passed`):** ROADMAP success criterion #2 explicitly says "visually verified on Windows Terminal (8+ colors legible)". The byte-level proof is shipped (snapshots show only named-16 variants), but the literal acceptance criterion includes a visual check on a specific platform that this autonomous verifier cannot perform. This is the smallest remaining gap.

---

_Verified: 2026-04-16_
_Verifier: Claude (eclusa-verifier)_
