---
phase: 2.2
slug: color-mode-pipeline
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-15
---

# Phase 2.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` + `insta` (snapshot fixtures) — both already configured |
| **Config file** | `Cargo.toml` per crate; `insta.yaml` at workspace root |
| **Quick run command** | `cargo test -p happyterminals-backend-ratatui color` |
| **Full suite command** | `cargo test --workspace && cargo test -p happyterminals-backend-ratatui --test color_snapshots` |
| **Estimated runtime** | ~20 seconds quick; ~60 seconds full |

---

## Sampling Rate

- **After every task commit:** `cargo test -p happyterminals-backend-ratatui color` + `cargo clippy -p happyterminals-backend-ratatui -- -D warnings`
- **After every plan wave:** `cargo test --workspace && cargo clippy --workspace -- -D warnings`
- **Before `/eclusa:verify-work`:** full suite green including 8 insta snapshots
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

*Populated during planning. Each must-have gate maps to exactly one owning task.*

| Must-Have Gate | Source | Test Type | Owner Plan → Task |
|----------------|--------|-----------|--------------------|
| `NO_COLOR=1` produces zero SGR color escape sequences on flush | RESEARCH §Must-Haves | integration | TBD by planner |
| `$COLORTERM=truecolor` detected as `ColorMode::TrueColor` (priority after `NO_COLOR`) | RESEARCH §Must-Haves | unit | TBD by planner |
| `FrameSpec.color_mode = Some(Ansi16)` on truecolor terminal emits only 4-bit codes | RESEARCH §Must-Haves | snapshot | TBD by planner |
| RGB→256 Euclidean lookup matches xterm canonical palette for all 6×6×6 cube corners + 24 greys | RESEARCH §Must-Haves | unit (const test) | TBD by planner |
| 256→16 lookup table builds at compile time (`const fn` verification) + round-trip stable | RESEARCH §Must-Haves | unit | TBD by planner |
| 8 `insta` snapshots (4 modes × 2 scenes) pass deterministically on CI | RESEARCH §Must-Haves | snapshot | TBD by planner |
| `cargo run --example color-test -- --force-color=none` produces monochrome output with modifiers preserved | RESEARCH §Must-Haves | integration + manual UAT | TBD by planner |
| Mono mode drops `fg`/`bg` but preserves `Modifier::BOLD` / `ITALIC` / `UNDERLINE` / `REVERSED` | RESEARCH §Must-Haves | unit | TBD by planner |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/happyterminals-backend-ratatui/src/color.rs` — new module hosting `ColorMode` enum, detection cascade, `downsample(&mut Buffer, ColorMode)`
- [ ] `crates/happyterminals-backend-ratatui/src/color/palette.rs` — `PALETTE_256` + `PALETTE_16_LUT` built as `const fn` arrays
- [ ] `crates/happyterminals-backend-ratatui/tests/color_snapshots.rs` — insta snapshot harness iterating the 8-fixture matrix
- [ ] `crates/happyterminals/examples/color-test/main.rs` — demo example with `--force-color` CLI arg
- [ ] `crates/happyterminals/README.md` §"Terminal Color Support" — docs block with tmux `Tc` guidance
- [ ] `happyterminals::prelude` re-exports for `ColorMode`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visible color degradation across modes | REND-08 | Perceptual check that colors downsample meaningfully, not wrong-hue | Run `cargo run --example color-test -- --force-color=truecolor`, then `=256`, `=16`, `=none` — confirm each mode looks progressively degraded but not incorrect |
| tmux `Tc` passthrough on live tmux session | RESEARCH §Must-Haves | Multi-process env propagation can't be CI-mocked | Inside tmux with `Tc` configured, run color-test without `--force-color`; should auto-detect TrueColor. Without `Tc`, should detect Palette256. |
| Windows Terminal + NO_COLOR respect | RESEARCH §Pitfalls | Platform-specific env handling | On Windows Terminal, `set NO_COLOR=1 && cargo run --example color-test` — confirm monochrome output |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (color module, palette, snapshot harness, example, docs)
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s
- [ ] `nyquist_compliant: true` set in frontmatter after planner populates Per-Task map

**Approval:** pending
