---
phase: 2.2
slug: color-mode-pipeline
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-15
updated: 2026-04-15
---

# Phase 2.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` + `insta` (snapshot fixtures) — both already configured |
| **Config file** | `Cargo.toml` per crate; `insta.yaml` at workspace root |
| **Quick run command** | `cargo test -p happyterminals-backend-ratatui color` |
| **Full suite command** | `cargo test --workspace && cargo test -p happyterminals-backend-ratatui --test color_snapshots` |
| **Estimated runtime** | ~20 seconds quick; ~60 seconds full |

---

## Sampling Rate

- **After every task commit:** `cargo test -p happyterminals-backend-ratatui color` + `cargo clippy -p happyterminals-backend-ratatui -- -D warnings`
- **After every plan wave:** `cargo test --workspace && cargo clippy --workspace -- -D warnings`
- **Before `/eclusa:verify-work`:** full suite green including 8 insta snapshots
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

*Each must-have gate is owned by exactly one task. Every gate has an automated verify command that runs in < 20 seconds.*

| Must-Have Gate | Source | Test Type | Owner Plan → Task |
|----------------|--------|-----------|--------------------|
| `NO_COLOR=1` produces zero SGR color escape sequences on flush | RESEARCH §Must-Haves | unit + integration | 02.2-01 → Task 2 (`downsample_mono_strips_*` inline tests) + 02.2-01 → Task 3 (`no_color_strips_all_chromatic` integration test) |
| `$COLORTERM=truecolor` detected as `ColorMode::TrueColor` (priority after `NO_COLOR`) | RESEARCH §Must-Haves | unit + integration | 02.2-01 → Task 2 (`colorterm_truecolor` inline) + 02.2-01 → Task 3 (`colorterm_truecolor` integration) |
| `FrameSpec.color_mode = Some(Ansi16)` on truecolor terminal emits only 4-bit codes | RESEARCH §Must-Haves | snapshot | 02.2-03 → Task 1 (`cube_ansi16` + `bunny_ansi16` snapshots confirm only 16-named `Color` variants) |
| RGB→256 Euclidean lookup matches xterm canonical palette for all 6×6×6 cube corners + 24 greys | RESEARCH §Must-Haves | unit (const test) | 02.2-01 → Task 1 (`palette_cube_origin`, `palette_cube_corner_white`, `palette_cube_mid_sample`, `palette_grey_first`, `palette_grey_last`, `nearest_256_*` inline tests in palette.rs) |
| 256→16 lookup table builds at compile time (`const fn` verification) + round-trip stable | RESEARCH §Must-Haves | unit | 02.2-01 → Task 1 (`palette_16_lut_range`, `palette_16_lut_white_maps_to_white` inline tests — compile-time build proven by `pub const PALETTE_16_LUT: [u8; 256] = build_16_lut()`) |
| 8 `insta` snapshots (4 modes × 2 scenes) pass deterministically on CI | RESEARCH §Must-Haves | snapshot | 02.2-03 → Task 1 (`cube_{truecolor,palette256,ansi16,mono}` + `bunny_{truecolor,palette256,ansi16,mono}`) |
| `cargo run --example color-test -- --force-color=none` produces monochrome output with modifiers preserved | RESEARCH §Must-Haves | integration + manual UAT | 02.2-03 → Task 2 (compile gate: `cargo build --example color-test`); manual UAT in 02.2-03 `<verification>` block |
| Mono mode drops `fg`/`bg` but preserves `Modifier::BOLD` / `ITALIC` / `UNDERLINE` / `REVERSED` | RESEARCH §Must-Haves | unit | 02.2-01 → Task 2 (`downsample_mono_preserves_modifier_bold`, `downsample_mono_preserves_modifier_underline_reverse` inline tests in color.rs) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/happyterminals-backend-ratatui/src/color.rs` — new module hosting `ColorMode` enum, detection cascade, `downsample(&mut Buffer, ColorMode)` → **owned by Plan 02.2-01 Task 2**
- [ ] `crates/happyterminals-backend-ratatui/src/color/palette.rs` — `PALETTE_256` + `PALETTE_16_LUT` built as `const fn` arrays → **owned by Plan 02.2-01 Task 1**
- [ ] `crates/happyterminals-backend-ratatui/tests/color_detect.rs` — env-cascade integration tests via `EnvProvider` injection (no `std::env::set_var`) → **owned by Plan 02.2-01 Task 3**
- [ ] `crates/happyterminals-backend-ratatui/tests/color_snapshots.rs` — insta snapshot harness iterating the 8-fixture matrix → **owned by Plan 02.2-03 Task 1**
- [ ] `crates/happyterminals-backend-ratatui/src/frame_spec.rs` — add `color_mode: Option<ColorMode>` field → **owned by Plan 02.2-02 Task 1**
- [ ] `crates/happyterminals-backend-ratatui/src/guard.rs` — cache `ColorMode` on `TerminalGuard` + add `acquire_with_color_mode` constructor → **owned by Plan 02.2-02 Task 1**
- [ ] `crates/happyterminals-backend-ratatui/src/run.rs` — wire `detect_color_mode_from_real_env` + `downsample` into both `run()` and `run_scene()` flush paths → **owned by Plan 02.2-02 Task 2**
- [ ] `crates/happyterminals/examples/color-test/main.rs` — demo example with `--force-color` CLI arg → **owned by Plan 02.2-03 Task 2**
- [ ] `crates/happyterminals/README.md` §"Terminal Color Support" — docs block with tmux `Tc` guidance → **owned by Plan 02.2-03 Task 3**
- [ ] `happyterminals::prelude` re-exports for `ColorMode` → **owned by Plan 02.2-02 Task 2**

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visible color degradation across modes | REND-08 | Perceptual check that colors downsample meaningfully, not wrong-hue | Run `cargo run --example color-test -- --force-color=truecolor`, then `=256`, `=16`, `=none` — confirm each mode looks progressively degraded but not incorrect |
| tmux `Tc` passthrough on live tmux session | RESEARCH §Must-Haves | Multi-process env propagation can't be CI-mocked | Inside tmux with `Tc` configured, run color-test without `--force-color`; should auto-detect TrueColor. Without `Tc`, should detect Palette256. |
| Windows Terminal + NO_COLOR respect | RESEARCH §Pitfalls | Platform-specific env handling | On Windows Terminal, `set NO_COLOR=1 && cargo run --example color-test` — confirm monochrome output |
| `NO_COLOR=1` + `--force-color=truecolor` → Mono at runtime (integration cross-check) | RESEARCH §Must-Haves (priority ordering) | Env+override interaction at the real-binary level; unit tests already cover this, the manual check is a belt-and-suspenders regression gate for 2.2's exit review | `NO_COLOR=1 cargo run --example color-test -p happyterminals -- --force-color=truecolor` — output must be monochrome (`NO_COLOR` wins over programmatic override) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (verified — every task across plans 01/02/03 has an `<automated>` command)
- [x] Wave 0 covers all MISSING references (color module, palette, snapshot harness, cascade integration test, frame_spec+guard+run wiring, prelude re-export, example, docs)
- [x] No watch-mode flags
- [x] Feedback latency < 20s
- [x] `nyquist_compliant: true` set in frontmatter (every must-have gate above has an explicit Owner Plan → Task)

**Approval:** ✅ ready for execute-phase (on Plan 02.2-01 first; 02.2-02 follows after 01 is green; 02.2-03 follows after 02)
