---
phase: 01.1-grid-ratatui-backend-static
plan: 02
subsystem: backend
tags: [ratatui, crossterm, terminal-guard, raii, panic-hook, event-mapping]

# Dependency graph
requires:
  - phase: 01.0-reactive-core
    provides: Signal/SignalSetter for cross-thread event writes
provides:
  - TerminalGuard RAII with panic hook (raw mode + alt screen + mouse + cursor + SGR reset)
  - InputEvent enum mapped from crossterm events
  - FrameSpec configuration (fps, title, mouse_capture)
  - is_quit_event Ctrl+C detection
affects: [01.1-03, 01.2-effects-pipeline]

# Tech tracking
tech-stack:
  added: [tokio (workspace dep), crossterm 0.29, ratatui 0.30, ratatui-crossterm 0.1]
  patterns: [RAII terminal guard, best-effort restore on drop/panic, event mapping layer]

key-files:
  created:
    - crates/happyterminals-backend-ratatui/src/guard.rs
    - crates/happyterminals-backend-ratatui/src/event.rs
    - crates/happyterminals-backend-ratatui/src/frame_spec.rs
  modified:
    - crates/happyterminals-backend-ratatui/Cargo.toml
    - crates/happyterminals-backend-ratatui/src/lib.rs
    - Cargo.toml

key-decisions:
  - "Added tokio as workspace dependency (was missing from workspace Cargo.toml)"
  - "map_event takes &CrosstermEvent (by reference) per clippy recommendation"
  - "InputEvent filters KeyEventKind::Repeat and Release, only passes Press events"

patterns-established:
  - "RAII guard pattern: acquire on creation, best-effort restore on Drop (all errors ignored)"
  - "Panic hook pattern: install_panic_hook() captures original hook, restores terminal first"
  - "Event mapping layer: crossterm types never leak beyond the backend crate"

requirements-completed: [BACK-02, BACK-03]

# Metrics
duration: 3min
completed: 2026-04-15
---

# Phase 1.1 Plan 02: Backend Infrastructure Summary

**TerminalGuard RAII with panic-safe restore, InputEvent mapping from crossterm, and FrameSpec render config at 30fps default**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-15T10:42:15Z
- **Completed:** 2026-04-15T10:45:48Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- TerminalGuard acquires raw mode + alt screen + mouse + cursor hide; restores all on Drop including SGR reset
- Panic hook ensures terminal restore runs before panic message prints
- InputEvent enum cleanly maps Key, Mouse, Resize, FocusGained, FocusLost from crossterm
- FrameSpec defaults to 30fps with mouse capture, provides frame_duration() for tick interval
- is_quit_event detects Ctrl+C for graceful shutdown
- 14 unit tests covering all code paths

## Task Commits

Each task was committed atomically:

1. **Task 1: Backend crate deps + TerminalGuard RAII + panic hook** - `bcb6c97` (feat)
2. **Task 2: FrameSpec + input event mapping module** - `ea1b100` (feat)

**Plan metadata:** (pending final commit)

## Files Created/Modified
- `Cargo.toml` - Added tokio as workspace dependency
- `crates/happyterminals-backend-ratatui/Cargo.toml` - Wired all runtime deps (core, ratatui, crossterm, tokio, tracing)
- `crates/happyterminals-backend-ratatui/src/guard.rs` - TerminalGuard RAII + install_panic_hook
- `crates/happyterminals-backend-ratatui/src/event.rs` - InputEvent enum + map_event + is_quit_event
- `crates/happyterminals-backend-ratatui/src/frame_spec.rs` - FrameSpec config struct with defaults
- `crates/happyterminals-backend-ratatui/src/lib.rs` - Module declarations + re-exports

## Decisions Made
- Added tokio to workspace Cargo.toml since plan referenced `tokio = { workspace = true }` but it was missing
- Changed `map_event` signature from by-value to by-reference per clippy lint (crossterm Event is Copy, passing by value was unnecessary)
- InputEvent only captures KeyEventKind::Press, filtering Repeat and Release to avoid double-triggering in signal consumers

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added tokio to workspace Cargo.toml**
- **Found during:** Task 1 (Backend crate deps)
- **Issue:** Plan instructed `tokio = { workspace = true }` in backend Cargo.toml, but tokio was not declared in the workspace `[workspace.dependencies]`
- **Fix:** Added `tokio = { version = "1", default-features = false, features = ["rt", "time", "macros", "sync"] }` to root Cargo.toml
- **Files modified:** Cargo.toml
- **Verification:** `cargo check -p happyterminals-backend-ratatui` succeeds
- **Committed in:** bcb6c97 (Task 1 commit)

**2. [Rule 1 - Bug] Changed map_event to take &CrosstermEvent**
- **Found during:** Task 2 (event mapping)
- **Issue:** clippy flagged `map_event(ev: CrosstermEvent)` as passing by value without consuming (CrosstermEvent is Copy)
- **Fix:** Changed to `map_event(ev: &CrosstermEvent)` with `match *ev`
- **Files modified:** crates/happyterminals-backend-ratatui/src/event.rs
- **Verification:** clippy -D warnings passes clean
- **Committed in:** ea1b100 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes necessary for compilation and lint compliance. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all modules are fully implemented with no placeholder data.

## Next Phase Readiness
- TerminalGuard, InputEvent, and FrameSpec are ready for the run() event loop in Plan 01.1-03
- All re-exports in place for downstream consumers

---
*Phase: 01.1-grid-ratatui-backend-static*
*Completed: 2026-04-15*
