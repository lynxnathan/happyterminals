---
phase: 03.2-json-recipe-loader-schema
plan: 02
type: execute
wave: 2
depends_on: [03.2-01]
files_modified:
  - crates/happyterminals-dsl/src/prelude.rs
  - crates/happyterminals/src/lib.rs
  - examples/recipes/hello.json
autonomous: false
requirements: [DSL-04, DSL-06]

must_haves:
  truths:
    - "load_recipe and recipe_schema are importable via happyterminals_dsl::prelude and the happyterminals meta-crate prelude"
    - "A sample recipe file exists that an LLM or human can use as a starting template"
    - "Loading the sample recipe file via load_recipe() produces a valid SceneIr without errors"
  artifacts:
    - path: "crates/happyterminals-dsl/src/prelude.rs"
      provides: "Re-exports of load_recipe, recipe_schema, RecipeError"
      contains: "load_recipe"
    - path: "examples/recipes/hello.json"
      provides: "Sample recipe file with orbit camera and cube"
      contains: "$version"
  key_links:
    - from: "crates/happyterminals-dsl/src/prelude.rs"
      to: "crates/happyterminals-dsl/src/json.rs"
      via: "pub use crate::json"
      pattern: "load_recipe"
    - from: "examples/recipes/hello.json"
      to: "crates/happyterminals-dsl/src/json.rs"
      via: "load_recipe() consumes the JSON"
      pattern: "\\$version"
---

<objective>
Wire the JSON recipe loader into the public API and provide a sample recipe file.

Purpose: Make load_recipe/recipe_schema discoverable via prelude imports, and give users/LLMs a concrete starting template. Human verification confirms the schema is LLM-friendly and the sample recipe loads correctly.

Output: Updated prelude, sample recipe JSON, integration test, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/PROJECT.md
@.eclusa/ROADMAP.md
@.eclusa/phases/03.2-json-recipe-loader-schema/03.2-CONTEXT.md
@.eclusa/phases/03.2-json-recipe-loader-schema/03.2-01-SUMMARY.md
@crates/happyterminals-dsl/src/prelude.rs
@crates/happyterminals-dsl/src/json.rs

<interfaces>
<!-- From Plan 01 outputs -->
From crates/happyterminals-dsl/src/json.rs:
```rust
pub fn load_recipe(json: &str) -> Result<SceneIr, RecipeError>;
pub fn recipe_schema() -> serde_json::Value;
pub fn scene_ir_to_recipe(ir: &SceneIr, camera: &CameraConfig) -> Recipe;
pub enum RecipeError { UnsupportedVersion { found: String }, SchemaValidation(String), Deserialize(String) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prelude re-exports + sample recipe + integration test</name>
  <files>crates/happyterminals-dsl/src/prelude.rs, examples/recipes/hello.json, crates/happyterminals-dsl/src/json.rs</files>
  <read_first>
    - crates/happyterminals-dsl/src/prelude.rs (current re-exports)
    - crates/happyterminals-dsl/src/json.rs (public API from Plan 01)
    - crates/happyterminals/src/lib.rs (meta-crate re-exports, if prelude exists)
  </read_first>
  <action>
    **Step 1: Update DSL prelude.** Add to `crates/happyterminals-dsl/src/prelude.rs`:
    ```
    pub use crate::json::{load_recipe, recipe_schema, RecipeError};
    ```

    **Step 2: Update meta-crate prelude.** Check `crates/happyterminals/src/lib.rs` for a prelude module. If it re-exports from happyterminals-dsl::prelude, the new symbols propagate automatically. If it cherry-picks, add `load_recipe, recipe_schema, RecipeError` explicitly.

    **Step 3: Create sample recipe.** Write `examples/recipes/hello.json`:
    ```json
    {
      "$version": "1.0",
      "camera": {
        "type": "orbit",
        "azimuth": 0.5,
        "elevation": 0.3,
        "distance": 5.0,
        "target": [0.0, 0.0, 0.0]
      },
      "layers": [
        {
          "z_order": 0,
          "children": [
            {
              "type": "cube",
              "transform": {
                "position": [0.0, 0.0, 0.0],
                "scale": [1.0, 1.0, 1.0]
              }
            }
          ]
        }
      ]
    }
    ```

    **Step 4: Add integration test** in json.rs (or a new test file) that reads `examples/recipes/hello.json` from disk via `include_str!` or `std::fs::read_to_string`, calls `load_recipe()`, and asserts success + SceneIr has 1 layer with 1 cube child.

    **Step 5: Print schema to stdout test.** Add a test that calls `recipe_schema()`, serializes to pretty JSON, and asserts it contains `"$version"`, `"camera"`, `"layers"` as property names. This verifies the schema is LLM-usable.

    **Step 6: Verify full build.** Run `cargo check --workspace` and `cargo test -p happyterminals-dsl` to confirm everything wires clean.
  </action>
  <acceptance_criteria>
    - grep -q 'load_recipe' crates/happyterminals-dsl/src/prelude.rs
    - grep -q 'recipe_schema' crates/happyterminals-dsl/src/prelude.rs
    - grep -q 'RecipeError' crates/happyterminals-dsl/src/prelude.rs
    - test -f examples/recipes/hello.json
    - grep -q '\\$version' examples/recipes/hello.json
    - grep -q 'hello.json\|hello_recipe' crates/happyterminals-dsl/src/json.rs
  </acceptance_criteria>
  <verify>
    <automated>cd /home/lynxnathan/code/happyterminals && cargo test -p happyterminals-dsl -- json --no-fail-fast 2>&1 | tail -20 && cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>
    - load_recipe and recipe_schema importable via `use happyterminals_dsl::prelude::*`
    - examples/recipes/hello.json exists with valid recipe format
    - Integration test loading hello.json passes
    - Schema output test confirms LLM-friendly property names
    - Workspace builds clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify JSON schema is LLM-friendly and recipe loads correctly</name>
  <files>examples/recipes/hello.json</files>
  <action>
    Human reviews the generated JSON schema and sample recipe to confirm:
    1. Schema is self-describing enough for an LLM to author valid recipes without Rust source
    2. Camera variant fields are clearly documented in schema oneOf
    3. Node types (cube, mesh, group) are clearly enumerated
    4. Sample recipe serves as a useful starting template
  </action>
  <what-built>
    JSON recipe loader with schema generation, validation, round-trip tests, sample recipe, and prelude wiring. The full DSL-04/DSL-06/DSL-07 implementation.
  </what-built>
  <how-to-verify>
    1. Run `cargo test -p happyterminals-dsl -- json` -- all tests should pass
    2. Inspect the generated schema: `cargo test -p happyterminals-dsl -- schema_output --nocapture 2>&1 | head -80`
       - Verify: camera variants clearly described with field names
       - Verify: node types (cube, mesh, group) documented
       - Verify: $version field required
    3. Read `examples/recipes/hello.json` -- is it clear enough that an LLM given just this + the schema could write new recipes?
    4. Optional: try writing a second recipe JSON by hand (2 layers, mesh node) and run it through load_recipe in a test
  </how-to-verify>
  <verify>
    <automated>cd /home/lynxnathan/code/happyterminals && cargo test -p happyterminals-dsl -- json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Human confirms schema is LLM-friendly (camera variants, node types, $version all clear)
    - Sample recipe is a useful template
  </done>
  <resume-signal>Type "approved" or describe issues with the schema/loader</resume-signal>
</task>

</tasks>

<verification>
All DSL tests pass: `cargo test -p happyterminals-dsl -- json`
Workspace compiles: `cargo check --workspace`
Sample recipe loads without error
Schema contains all expected properties
Prelude exports discoverable
</verification>

<success_criteria>
- load_recipe and recipe_schema available via prelude
- Sample recipe file exists and loads successfully
- Human confirms schema is LLM-friendly
- Full workspace builds clean
</success_criteria>

<output>
After completion, create `.eclusa/phases/03.2-json-recipe-loader-schema/03.2-02-SUMMARY.md`
</output>
