---
phase: 01.2-pipeline-tachyonfx-adapter
plan: 01
subsystem: pipeline
tags: [effect-trait, pipeline-executor, tachyonfx, thiserror, ratatui-core]

# Dependency graph
requires:
  - phase: 01.1-grid-ratatui-backend
    provides: Grid type with Deref<Target=Buffer>, put_str(), resize()
provides:
  - Effect trait with apply/is_done/reset/name contract
  - EffectState enum (Running/Done)
  - Pipeline executor (Vec<Box<dyn Effect>>, sequential application)
  - PipelineError enum with thiserror
  - Fx alias (tachyonfx::Effect re-exported as Fx)
  - Grid::buffer_mut() public method for cross-crate mutable Buffer access
affects: [01.2-02-tachyon-adapter, 01.3-3d-renderer, 01.4-scene-graph, 01.5-spinning-cube-demo]

# Tech tracking
tech-stack:
  added: [tachyonfx 0.25, thiserror 2.0]
  patterns: [effect-trait-object, sequential-pipeline, builder-with-pattern]

key-files:
  created:
    - crates/happyterminals-pipeline/src/effect.rs
    - crates/happyterminals-pipeline/src/error.rs
    - crates/happyterminals-pipeline/src/pipeline.rs
    - crates/happyterminals-pipeline/benches/pipeline_overhead.rs
  modified:
    - crates/happyterminals-core/src/grid.rs
    - crates/happyterminals-pipeline/Cargo.toml
    - crates/happyterminals-pipeline/src/lib.rs

key-decisions:
  - "Renamed add()/add_boxed() to with()/with_boxed() to avoid clippy pedantic confusion with std::ops::Add"
  - "Deprecated inner_mut() in favor of buffer_mut() for cross-crate buffer access"

patterns-established:
  - "Effect trait pattern: apply(&mut self, grid: &mut Grid, dt: Duration) -> EffectState"
  - "Pipeline builder: Pipeline::new().with(effect1).with(effect2)"
  - "Fx alias convention: tachyonfx::Effect re-exported as Fx to avoid name clash"

requirements-completed: [PIPE-01, PIPE-02, PIPE-04, PIPE-05, PIPE-07, DSL-03]

# Metrics
duration: 5min
completed: 2026-04-15
---

# Phase 1.2 Plan 01: Pipeline Foundation Summary

**Effect trait, Pipeline executor with sequential application, Fx alias resolving tachyonfx name clash, and Grid::buffer_mut() for adapter access**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-15T12:23:45Z
- **Completed:** 2026-04-15T12:28:25Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Effect trait with exact D-01 signature: `apply(&mut self, grid: &mut Grid, dt: Duration) -> EffectState`
- Pipeline executor holds `Vec<Box<dyn Effect>>`, runs effects sequentially with skip-done logic (D-06, D-07)
- Fx alias re-exports `tachyonfx::Effect` to resolve name clash before any consumer code (D-02)
- Grid::buffer_mut() provides public cross-crate mutable Buffer access for TachyonAdapter (Plan 02)
- PipelineError with thiserror for structured error handling (D-09)
- 16 total tests passing, all clippy clean with `-D warnings`

## Task Commits

Each task was committed atomically:

1. **Task 1: Effect trait, EffectState, PipelineError, Fx alias, Grid::buffer_mut()** - `b7bbd8b` (feat)
2. **Task 2: Pipeline executor with sequential effect application** - `34da147` (feat)
3. **Cargo.lock update** - `516b972` (chore)

## Files Created/Modified
- `crates/happyterminals-pipeline/src/effect.rs` - Effect trait + EffectState enum with 6 unit tests
- `crates/happyterminals-pipeline/src/error.rs` - PipelineError enum with thiserror, 2 display tests
- `crates/happyterminals-pipeline/src/pipeline.rs` - Pipeline executor struct with 10 unit tests
- `crates/happyterminals-pipeline/src/lib.rs` - Crate root with Fx alias, module declarations, re-exports
- `crates/happyterminals-pipeline/Cargo.toml` - Dependencies: core, tachyonfx, ratatui-core, thiserror; dev-deps: criterion, proptest
- `crates/happyterminals-pipeline/benches/pipeline_overhead.rs` - Criterion bench stub for Plan 02
- `crates/happyterminals-core/src/grid.rs` - Added buffer_mut(), deprecated inner_mut(), added test

## Decisions Made
- Renamed `add()`/`add_boxed()` to `with()`/`with_boxed()` on Pipeline builder to satisfy clippy pedantic lint (`should_implement_trait` for confusable `std::ops::Add::add`)
- Deprecated `inner_mut()` as `pub(crate)` with `#[deprecated]` attribute pointing to `buffer_mut()` for backward compat
- Used `RefCell` instead of `Cell` for mock effect call log in tests (Vec is not Copy)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Renamed add() to with() for clippy compliance**
- **Found during:** Task 2 (Pipeline executor)
- **Issue:** clippy::pedantic `should_implement_trait` lint flagged `add()` as confusable with `std::ops::Add::add`
- **Fix:** Renamed `add()` to `with()` and `add_boxed()` to `with_boxed()` -- idiomatic builder naming
- **Files modified:** crates/happyterminals-pipeline/src/pipeline.rs
- **Verification:** `cargo clippy -p happyterminals-pipeline -- -D warnings` passes
- **Committed in:** 34da147

---

**Total deviations:** 1 auto-fixed (1 bug/lint)
**Impact on plan:** Method name change only, no API or behavioral impact. Builder pattern still works identically.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Known Stubs
- `crates/happyterminals-pipeline/benches/pipeline_overhead.rs` - Benchmark placeholder, will be implemented in Plan 02 with TachyonAdapter and 10 wired effects

## Next Phase Readiness
- Effect trait and Pipeline executor are ready for TachyonAdapter implementation in Plan 02
- Grid::buffer_mut() provides the mutable Buffer access TachyonAdapter needs
- Fx alias is committed, preventing name clash in any downstream consumer
- Criterion bench harness is scaffolded, ready for real benchmarks in Plan 02

## Self-Check: PASSED

All 7 key files exist. All 3 commit hashes verified in git log.

---
*Phase: 01.2-pipeline-tachyonfx-adapter*
*Completed: 2026-04-15*
