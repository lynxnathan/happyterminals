---
phase: 02.5-resize-hardening-+-msrv-+-stl
verified: 2026-04-16T23:45:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 02.5: Resize Hardening + MSRV + STL Verification Report

**Phase Goal:** Close M2 -- resize race hardening on Windows Terminal, MSRV pin in CI, STL mesh support.
**Verified:** 2026-04-16T23:45:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | STL files load into Mesh with correct vertex/normal counts, never panic | VERIFIED | `load_stl()` at mesh.rs:345-448; cube.stl loads 12 tris, small_mesh.stl loads 4 tris; 11 STL tests pass |
| 2 | Invalid/corrupt STL files return MeshError::StlParse, never panic | VERIFIED | StlParse variant at mesh.rs:124; proptest fuzz (256 cases) on arbitrary bytes in stl_corpus.rs:106-110; nonexistent file returns Io error |
| 3 | Degenerate STL normals fall back to flat_normal() recomputation | VERIFIED | mesh.rs:417-432: checks `stl_normal.is_finite() && normal_len > 1e-12`, else calls `flat_normal(va, vb, vc)` |
| 4 | CI builds green on MSRV 1.88 and stable | VERIFIED | ci.yml test matrix: `["1.88", "stable"]` at line 51 |
| 5 | CI YAML references 1.88 everywhere (not 1.86) | VERIFIED | 5 occurrences of "1.88" (fmt, clippy, test matrix, docs, hygiene); 0 occurrences of "1.86" |
| 6 | Rapid resize events produce no panics and no garbled frames | VERIFIED | resize_pending skip-frame flag in all 3 run variants; grid.resize() called before skip check |
| 7 | Grid dimensions always match terminal size after resize settles | VERIFIED | `grid.resize(frame.area())` always called before resize_pending check (lines 84, 186, 319) |
| 8 | One frame is skipped after resize (imperceptible 16-33ms gap) | VERIFIED | resize_pending=true on resize event; next tick skips render callback, clones grid buffer, returns early |
| 9 | All three run variants (run, run_with_input, run_scene) are hardened | VERIFIED | 13 occurrences of resize_pending: 3 declarations (lines 77, 179, 309), 3 set-true (114, 231, 361), 3 check+reset in draw closure, 1 tick_fn guard in run_scene (line 315) |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/happyterminals-renderer/src/mesh.rs` | load_stl + MeshError::StlParse | VERIFIED | `pub fn load_stl` at line 345; `StlParse(String)` at line 124; 130+ lines of real implementation |
| `crates/happyterminals-renderer/tests/stl_corpus.rs` | STL corpus tests + proptest fuzz | VERIFIED | 111 lines: 5 tests + proptest fuzz harness (256 cases) |
| `crates/happyterminals-renderer/tests/fixtures/stl/cube.stl` | Minimal STL test fixture | VERIFIED | File exists; loads as 12 triangles, 8 vertices |
| `crates/happyterminals-renderer/tests/fixtures/stl/small_mesh.stl` | Small mesh fixture | VERIFIED | File exists; loads as 4 triangles |
| `.github/workflows/ci.yml` | CI with correct MSRV 1.88 | VERIFIED | 5x "1.88", 0x "1.86" |
| `crates/happyterminals-backend-ratatui/src/run.rs` | Resize-hardened event loop | VERIFIED | 13 resize_pending references across 3 run variants |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| mesh.rs | stl_io::read_stl | stl_io crate dependency | WIRED | `stl_io::read_stl(&mut file)` at mesh.rs:348 |
| Cargo.toml | workspace stl_io dep | stl_io.workspace = true | WIRED | `stl_io = { workspace = true }` at line 20 |
| run.rs | InputEvent::Resize | resize_pending flag set on resize, checked on tick | WIRED | Set at lines 114, 231, 361; checked at lines 85, 187, 321 |
| lib.rs | mesh::load_stl | public re-export | WIRED | `load_stl` exported (confirmed via grep) |
| stl_corpus.rs | load_stl | test import | WIRED | `use happyterminals_renderer::{load_stl, MeshError}` at line 13 |

### Data-Flow Trace (Level 4)

Not applicable -- phase artifacts are a loader function (mesh.rs), test suite (stl_corpus.rs), CI config (ci.yml), and event-loop hardening (run.rs). No dynamic rendering components added.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| STL tests pass | `cargo test -p happyterminals-renderer -- stl -q` | 11 passed, 92 filtered out | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| REND-10 | Plan 01, Plan 02 | STL mesh loading via stl_io | SATISFIED | load_stl() implemented with full error handling, DoS guards, degenerate normal fallback; 11 tests + proptest fuzz |
| REL-03 | Plan 01 | MSRV policy documented; CI tests MSRV + stable | SATISFIED | CI YAML: 5x "1.88", test matrix includes MSRV + stable; rust-toolchain.toml and Cargo.toml both at 1.88 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| run.rs | 424 | TODO (pre-existing) | Info | Not phase 02.5 scope; relates to future Renderer::draw refactor to accept &dyn Camera |

No blockers. No stubs. No placeholders in phase-modified files.

### Human Verification Required

### 1. Resize Behavior on Windows Terminal

**Test:** Run the model-viewer example on Windows Terminal, rapidly resize the window by dragging corners.
**Expected:** No garbled frames, no panics. At most a single imperceptible blank frame (16-33ms) after each resize, then clean rendering resumes.
**Why human:** Resize race conditions are timing-dependent and terminal-specific; cannot be reproduced in automated tests.

### 2. STL Model in Model-Viewer

**Test:** Load a .stl file in the model-viewer example (if CLI supports it yet).
**Expected:** Mesh renders with correct geometry and lighting.
**Why human:** Visual correctness of rendered mesh requires human judgment.

### Gaps Summary

No gaps found. All 9 observable truths verified. All artifacts exist, are substantive, and are properly wired. Both requirements (REND-10, REL-03) satisfied. All 4 commits verified in git history.

---

_Verified: 2026-04-16T23:45:00Z_
_Verifier: Claude (eclusa-verifier)_
