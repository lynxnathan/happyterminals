---
phase: 01.5-spinning-cube-demo-cross-terminal-matrix-m1-exit
verified: 2026-04-15T19:30:00Z
status: human_needed
score: 6/7 must-haves verified
re_verification: false
human_verification:
  - test: "Run spinning cube demo on accessible terminals from HUMAN-UAT.md matrix"
    expected: "Cube renders, rotates smoothly, coalesce effect plays, Ctrl-C exits clean"
    why_human: "Cross-terminal visual rendering and input behavior cannot be verified programmatically"
  - test: "Verify animated GIF/asciicast for README DEMO-02"
    expected: "Spinning cube recording embedded in README.md"
    why_human: "Recording requires screen capture tool; explicitly deferred per CONTEXT.md as nice-to-have"
---

# Phase 1.5: Spinning Cube Demo / Cross-Terminal Matrix / M1 Exit -- Verification Report

**Phase Goal:** Deliver the M1 exit artifact: spinning-cube example < 100 LOC, wire Scene into run(), 1-cell bytes hard gate, README update, cross-terminal verification.
**Verified:** 2026-04-15T19:30:00Z
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | run_scene() drives a Scene through the full stack: signal -> camera -> renderer -> pipeline -> grid -> ratatui | VERIFIED | `run.rs` lines 132-224: `scene.into_parts()` -> walk_and_render -> `renderer.draw()` -> `pipe.run_frame()` -> `frame.buffer_mut()` |
| 2 | Spinning cube example compiles and runs under 100 LOC | VERIFIED | `wc -l` = 43 LOC; `cargo build --example spinning-cube` succeeds |
| 3 | 1-cell bytes test passes: tiny signal change produces <= 50 bytes of ANSI output | VERIFIED | `cargo test --test scene_bytes_test` passes; assert `delta <= 50` at line 140 |
| 4 | Ctrl-C leaves terminal clean (inherited from TerminalGuard) | VERIFIED | `run_scene()` uses `install_panic_hook()` + `TerminalGuard::acquire()` (lines 140-142), same pattern as `run()` |
| 5 | README.md showcases the spinning cube as the headline feature | VERIFIED | README has "Spinning Cube Demo" section, hello world (18 lines, under 25), features, installation, crate table, MIT OR Apache-2.0 license |
| 6 | README contains a working <=25-line hello world code snippet | VERIFIED | 18-line rust snippet uses `scene()`, `run_scene`, `Signal`, `OrbitCamera` -- matches DSL test pattern |
| 7 | Cross-terminal verification checklist exists for user to execute | VERIFIED | HUMAN-UAT.md covers all 9 terminals (Windows Terminal, GNOME, Terminal.app, iTerm2, Kitty, Alacritty, tmux, screen, SSH) |

**Score:** 7/7 truths verified (automated checks pass; human testing pending for BACK-05 and DEMO-02 GIF)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/happyterminals-scene/src/prop.rs` | PropValue::read_untracked() | VERIFIED | Line 92: `pub fn read_untracked<T: Clone + 'static>` |
| `crates/happyterminals-scene/src/scene.rs` | Scene::into_parts() | VERIFIED | Line 80: `pub fn into_parts(self) -> (SceneIr, CameraConfig, Option<Pipeline>)` |
| `crates/happyterminals-backend-ratatui/src/run.rs` | run_scene() + walk_and_render + render_node | VERIFIED | Lines 132, 227, 241: all three functions present and substantive |
| `crates/happyterminals/examples/spinning-cube/main.rs` | M1 exit demo < 100 LOC | VERIFIED | 43 LOC, uses DSL builder, Signal, OrbitCamera, Pipeline, run_scene |
| `crates/happyterminals-backend-ratatui/tests/scene_bytes_test.rs` | DEMO-04 hard gate test | VERIFIED | `scene_one_cell_change_minimal_bytes` test passes, asserts `<= 50` bytes |
| `README.md` | Spinning cube showcase + hello world | VERIFIED | Full rewrite with demo section, 18-line hello world, crate table, license |
| `HUMAN-UAT.md` | Cross-terminal verification matrix | VERIFIED | 9 terminals, verification criteria, sign-off section |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `run.rs` | `scene.rs` | `Scene::into_parts()` | WIRED | Line 162: `let (ir, camera_config, mut pipeline) = scene.into_parts()` |
| `run.rs` | `prop.rs` | `PropValue::read_untracked()` | WIRED | Line 253: `prop.read_untracked::<f32>()` in render_node |
| `run.rs` | `renderer/lib.rs` | `Renderer::draw()` | WIRED | Line 257: `renderer.draw(grid, camera, projection, shading)` |
| `spinning-cube/main.rs` | `run.rs` | `run_scene()` call | WIRED | Line 31: `run_scene(scene, FrameSpec {...}, \|dt, _input\| {...})` |
| `backend/lib.rs` | `run.rs` | `pub use run_scene` | WIRED | Line 18: `pub use run::{run, run_scene}` |
| `happyterminals/lib.rs` | `backend/lib.rs` | meta crate re-export | WIRED | Line 29: `pub use happyterminals_backend_ratatui::{run, run_scene, ...}` |
| `README.md` | `hello_world.rs` | DSL pattern match | WIRED | README snippet uses `scene()`, `.camera()`, `.layer()`, `.build()`, `run_scene` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `run.rs::run_scene` | `ir` (SceneIr) | `scene.into_parts()` | Yes -- DSL builder constructs real nodes | FLOWING |
| `run.rs::render_node` | `camera.azimuth` | `prop.read_untracked::<f32>()` | Yes -- reads from Signal reactively | FLOWING |
| `run.rs::walk_and_render` | `grid` | `renderer.draw()` writes ASCII cells | Yes -- z-buffer rasterizer | FLOWING |
| `scene_bytes_test.rs` | `rotation` Signal | `Signal::new(0.0)` then `rotation.set(0.001)` | Yes -- real signal update | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Spinning cube compiles | `cargo build --example spinning-cube` | Success (0.11s) | PASS |
| Spinning cube under 100 LOC | `wc -l examples/spinning-cube/main.rs` | 43 lines | PASS |
| Scene bytes test passes (DEMO-04 hard gate) | `cargo test --test scene_bytes_test` | 1 passed (0.02s) | PASS |
| Full workspace tests pass | `cargo test --workspace` | 165 passed, 6 ignored | PASS |
| Clippy clean | `cargo clippy --workspace -- -D warnings` | No issues found | PASS |
| README has no forbidden strings | `grep -ci "tui-vfx\|Haskell" README.md` | 0 matches | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DEMO-01 | 01.5-01 | spinning-cube example < 100 LOC with effect at 30fps | SATISFIED | 43 LOC, compiles, uses coalesce effect, FrameSpec defaults to 30fps |
| DEMO-02 | 01.5-02 | README with spinning cube GIF/asciicast + hello world | PARTIAL | README has hello world (18 lines) and showcase section; GIF deferred per CONTEXT.md as nice-to-have |
| DEMO-03 | 01.5-01 | Ctrl-C leaves terminal clean on all platforms | SATISFIED | run_scene uses install_panic_hook + TerminalGuard::acquire (same proven pattern as run()) |
| DEMO-04 | 01.5-01 | 1-cell change output test: <= ~50 bytes, not full repaint | SATISFIED | scene_one_cell_change_minimal_bytes test passes with `assert!(delta <= 50)` |
| BACK-05 | 01.5-03 | Cross-terminal verification matrix, spinning cube on 9 terminals | NEEDS HUMAN | HUMAN-UAT.md created with all 9 terminals; user must execute and fill results |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| README.md | 16 | `<!-- TODO: Replace with animated GIF or asciinema recording -->` | Info | Known deferral per CONTEXT.md; GIF is nice-to-have, not blocking M1 |

No stubs, no empty implementations, no placeholder components found in any phase artifact.

### Human Verification Required

### 1. Cross-Terminal Spinning Cube (BACK-05)

**Test:** Run `cargo run -p happyterminals --example spinning-cube` on each accessible terminal from HUMAN-UAT.md matrix (Windows Terminal, GNOME Terminal, Terminal.app, iTerm2, Kitty, Alacritty, tmux, screen, SSH)
**Expected:** Cube renders with ASCII characters, rotates smoothly at ~30fps, coalesce startup effect plays, no flicker/tearing, Ctrl-C exits cleanly leaving terminal in sane state
**Why human:** Visual rendering quality, frame smoothness, and terminal state after exit cannot be verified programmatically

### 2. README GIF/Asciicast (DEMO-02 partial)

**Test:** Record an asciinema/GIF of the spinning cube demo and embed in README.md
**Expected:** Visual demonstration of the spinning cube in the README
**Why human:** Requires screen capture tooling; explicitly deferred as nice-to-have per CONTEXT.md

### Gaps Summary

No blocking gaps found. All automated M1 exit gates pass:

1. Spinning cube example: EXISTS, 43 LOC (under 100), COMPILES
2. Scene-level 1-cell bytes test: PASSES (delta <= 50 bytes)
3. README: Updated with showcase + 18-line hello world + license
4. HUMAN-UAT.md: EXISTS with all 9 terminals
5. run_scene() wires full stack: Scene -> into_parts -> walk_and_render -> Renderer::draw -> Pipeline::run_frame -> Grid -> ratatui frame buffer

The two items needing human action are:
- BACK-05: User must actually run the demo on terminals and fill HUMAN-UAT.md (checklist ready)
- DEMO-02: GIF recording is deferred per CONTEXT.md (nice-to-have, not blocking M1)

---

_Verified: 2026-04-15T19:30:00Z_
_Verifier: Claude (eclusa-verifier)_
