---
phase: 01.5-spinning-cube-demo-cross-terminal-matrix-m1-exit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DEMO-02]

must_haves:
  truths:
    - "README.md showcases the spinning cube as the headline feature"
    - "README contains a working <=25-line hello world code snippet"
    - "README includes installation instructions and crate links"
  artifacts:
    - path: "README.md"
      provides: "Project README with spinning cube showcase and hello world"
      contains: "happyterminals"
  key_links:
    - from: "README.md"
      to: "crates/happyterminals-dsl/tests/hello_world.rs"
      via: "Hello world snippet matches DSL test pattern"
      pattern: "scene\\(\\)"
---

<objective>
Update the root README.md to showcase the spinning cube demo and include a <=25-line hello world snippet.

Purpose: DEMO-02 requires the README to be the project's front door — showcasing what happyterminals does with a compelling demo and a copy-pasteable hello world.

Output: Updated `README.md` with project description, spinning cube showcase (GIF placeholder), hello world snippet, installation instructions, and crate links.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/PROJECT.md
@.eclusa/ROADMAP.md
@.eclusa/phases/01.5-spinning-cube-demo-cross-terminal-matrix-m1-exit/01.5-CONTEXT.md

@crates/happyterminals-dsl/tests/hello_world.rs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with spinning cube showcase and hello world (per D-08, D-09)</name>
  <files>README.md</files>
  <read_first>
    README.md,
    crates/happyterminals-dsl/tests/hello_world.rs,
    .eclusa/PROJECT.md,
    crates/happyterminals/src/lib.rs
  </read_first>
  <action>
    Rewrite `README.md` completely. The current README is stale (from Phase 0, references tui-vfx per CONTEXT.md specifics section).

    Structure (per D-08):
    1. **Header**: `# happyterminals` with tagline matching PROJECT.md core value: "Terminal art should feel like magic, not plumbing."
    2. **Spinning cube showcase**: A section with a placeholder for the animated GIF/asciicast. Use a comment or placeholder image indicating where the recording will go. Text: "A signal-rotated, effect-enhanced ASCII cube rendered entirely in your terminal." The GIF itself is deferred (out of scope per CONTEXT.md).
    3. **Hello World**: A <=25-line code snippet (per D-09) matching the DSL pattern from `hello_world.rs`. Use `happyterminals::prelude::*`. The snippet should show: create_root, Signal, scene() builder with camera + layer + cube + rotation, build, and run_scene. Count lines carefully — must be <= 25 lines including imports and main function.

       Reference snippet from hello_world.rs:
       ```rust
       use happyterminals::prelude::*;

       #[tokio::main(flavor = "current_thread")]
       async fn main() -> Result<(), Box<dyn std::error::Error>> {
           let (result, _owner) = create_root(|| {
               let rotation = Signal::new(0.0_f32);
               let r = rotation.clone();
               let scene = scene()
                   .camera(OrbitCamera::default())
                   .layer(0, |l| l.cube().rotation(&rotation))
                   .build()?;
               Ok((scene, r))
           });
           let (scene, rotation) = result?;
           run_scene(scene, FrameSpec::default(), |dt, _| {
               rotation.set(rotation.untracked() + dt.as_secs_f32());
           }).await
       }
       ```
       That is ~18 lines — well within 25.

    4. **Features**: Brief bullet list of what the stack provides (fine-grained reactive signals, 3D ASCII rendering, tachyonfx effects pipeline, scene graph DSL, cross-terminal support).
    5. **Installation**: `cargo add happyterminals` + note about requiring tokio runtime.
    6. **Crate structure**: Table of the 7 crates with brief descriptions and links.
    7. **License**: Dual MIT OR Apache-2.0 (per HYG-05).

    Do NOT include:
    - References to tui-vfx, Haskell bindings, or any forbidden strings
    - Animated GIF (deferred — use placeholder text only)
    - Feature descriptions for M2+ items (OBJ loading, particles, color pipeline, JSON recipes, Python bindings)

    Tone: Excited but professional. This is a creative tool for terminal artists.
  </action>
  <verify>
    <automated>! grep -qi "tui-vfx\|Haskell" README.md && grep -q "happyterminals" README.md && grep -q "scene()" README.md && grep -q "run_scene\|run" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists and is non-empty
    - grep -q "happyterminals" README.md
    - grep -q "scene()" README.md
    - grep -q "Signal" README.md
    - grep -qi "hello.world\|getting.started\|quick.start" README.md
    - grep -q "MIT OR Apache-2.0" README.md
    - no forbidden strings: ! grep -qi "tui-vfx" README.md
    - no forbidden strings: ! grep -qi "Haskell" README.md
  </acceptance_criteria>
  <done>
    README.md is rewritten with: project tagline, spinning cube showcase section (GIF placeholder), <=25-line hello world code snippet matching DSL pattern, feature bullets, installation instructions, crate structure table, and dual license notice. No forbidden strings present.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "scene()" README.md` returns >= 1
2. No forbidden strings: `grep -ci "tui-vfx\|Haskell" README.md` returns 0
3. Hello world snippet present and <= 25 lines
</verification>

<success_criteria>
- README.md showcases happyterminals with the spinning cube as the headline feature
- Hello world snippet is <= 25 lines and matches DSL pattern
- No stale references to tui-vfx or Haskell
- Installation instructions and crate links present
</success_criteria>

<output>
After completion, create `.eclusa/phases/01.5-spinning-cube-demo-cross-terminal-matrix-m1-exit/01.5-02-SUMMARY.md`
</output>
